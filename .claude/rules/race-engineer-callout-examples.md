---
paths:
  - "packages/event-bus/**"
  - "packages/sim-events-iracing/**"
  - "packages/audio-scenarios/**"
  - "packages/audio-assets/**"
  - "packages/scenario-harness/**"
  - "packages/deck-core/src/global-settings.ts"
  - "packages/iracing-actions/src/actions/pit-crew/**"
  - "packages/iracing-plugin-stream-deck/src/plugin.ts"
  - "packages/iracing-plugin-mirabox/src/plugin.ts"
---
# Race Engineer Callout — Reference Implementations

Worked precedents for the how-to in `@.claude/rules/race-engineer-callouts.md`. Each entry names the pattern a past callout established and the reusable lesson — consult one when a new callout needs a variation the checklist doesn't cover (continuous-distance triggers, multi-class projection, replay gating, cause classification, …). The full rationale for any entry also lives in its linked issue/PR.

- **Track Conditions / Wetness change** — issue #526. End-to-end example covering a brand-new family with a new bus enum, directional predicate, single per-callout opt-in covering multiple subjects.
- **Flag callouts** — issue #467. Per-subject opt-in (one boolean per flag colour); scope-aware predicate (`yellow.scope`); session-type branching (green/white/checkered).
- **Pit-service status** — issue #479. Per-target opt-in across eight subjects; sentinel suppression (* → None).
- **Damage** — issue #489. Single-subject family; debounced rising-edge detection in the diff.
- **Pit readback** — issues #476 / #481. Compositional scenarios (multiple pools per fire) and snapshot-at-fire-time pattern.
- **Session start** — issue #542. Single-subject family on the existing `session.changed` event (practice/qualifying sessions only); snapshot-at-fire-time conditions (`getSessionStartConditions()`) composed with the PI driver-name pick in each plugin; dynamic clips resolved via `engine.defineVar` (`registerSessionStartVars`) rather than enumerated `if` branches; a conditional clause (pit speed) gated on a curated value set. Establishes the **snapshot-driven builder-export shape** (issue #558): exports `buildSessionStartScenario(getSnapshot)` + `registerSessionStartVars(...)` rather than a static `*_ALERTS` array, because the scenario's `where:` / `if` steps close over a runtime resolver and must be materialized at wiring time — a future snapshot-driven family copies this precedent. **#668 moved the trigger** from `driver.firstOnTrack` to `session.changed` (~3 s after the session begins, before the driver leaves the garage) with a scenario-level `triggerDelay` providing the pause; the translator's fresh-connect synthesis was extended to cover practice/qualifying sessions (previously only covered race-start on connect). The #568 race-start scenario's `where:` already restricts itself to race sessions, so session-start's `where:` only needed to keep rejecting race — the two scenarios ride the same event without double-greeting.
- **UI-side acknowledgment (Race Engineer toggle)** — issue #554. Not a sim-event-driven callout — the scenario engine isn't involved. The Pit Crew action's `toggleRaceEngineer()` plays "going silent" / "resuming" directly via `playVoiceSequence(...)` on `AudioChannel.Voice`, gated by a per-callout opt-in (`calloutEnabledToggleRaceEngineer`). Reuses the same `in-flight` bypass shape as the PI Voice Test (`raceEngineerToggleInFlight` ↔ `raceEngineerTestInFlight`) so `applyRaceEngineerAudio` leaves Voice audible after the master gate flips off, until the ack clip finishes. Reach for this pattern when a Stream Deck button itself needs to speak a confirmation; reach for the scenario-engine pattern when telemetry should drive the speech.
- **SDK-connection ack (Telemetry-connect radio check)** — issue #554 follow-up. Triggered by a real-time signal that isn't on the event-bus catalog: the SDK controller's connection state (false→true transition). Each Pit Crew instance subscribes to `sdkController` with its own id (`pitCrewRadioCheck:<contextId>`); a module-scope `lastTelemetryConnected: boolean | null` dedups so the ack fires once per real transition no matter how many Pit Crew buttons happen to be mounted. Disconnect flips the tracker back so reconnects re-fire. Composes the active driver-name clip + a new `toggle/radio-check-01` clip via `playVoiceSequence(...)`. No in-flight bypass needed (master gate is on by definition when this fires; gate-off short-circuits before playback). Use this pattern when a non-event-bus signal (connection state, app monitor, settings change) should drive a one-shot callout, with care to dedup across action instances at module scope.
- **Lap time (best lap)** — issue #555. Single-subject family on a new `lap.completed` bus event with a rich payload (`{ lap, lapTime, isBest, isFirstValid, bestLapTime?, previousBestLapTime?, lapsRemaining?, timeRemaining?, sessionType? }`). Time readout composed from `lap-time-intro` + `lap-time-minute` + `lap-time-second` + `lap-time-decimal` clips via `engine.defineVar` (`registerLapTimeVars`); a conditional `{ if }` step skips the minute clip for sub-1-minute laps; `where:` filters on `isBest && lapTimeIsSpeakable(lapTime)` so off-range laps stay silent rather than producing a partial readout. The snapshot resolver is **plugin-owned** (a `lap.completed` subscription that caches the latest payload) rather than translator-owned, because the data is frozen in the event itself rather than read from live telemetry. Triggered by `LapLastLapTime` changing (the field iRacing refreshes when a lap completes) rather than `LapCompleted` incrementing — the counter races the time-field refresh and produces stale duplicate emissions. Establishes the **snapshot-driven builder-export shape** (issue #558): exports `buildLapTimeScenario(getSnapshot, getRaceFinishedFired?)` + `registerLapTimeVars(...)` rather than a static `*_ALERTS` array, because the scenario's `where:` / `if` steps close over a runtime resolver and must be materialized at wiring time — a future snapshot-driven family copies this precedent.
- **Race start** — issue #568. Single-subject family that **replaces** session-start in race sessions: the existing session-start scenario's `where:` returns false when `sessionType === "race"`, and race entries are spoken exclusively by the new race-start scenario fired off `session.changed` (~3 s after the transition lands, before the driver even has to leave the garage). Reuses an existing bus event rather than introducing a new one — the trigger is `session.changed` filtered by `classifySessionType(getSessionType()) === "race"`, anchored on the per-session reset hook (`resetPerSessionState`) from #564. Demonstrates **clip-group reuse across families** — the conditions brief (track temp / air temp / wetness intros, integer temp clips, wetness state clips, degrees-unit clips) re-points at the existing `session-start`, `session-start-temp-numbers`, and `position-number` groups so the new clips ship as only 3 entries (`time-to-race-01`, `starting-from-pole-01`, `qualifying-put-us-to-01`). Position clause uses a **two-branch conditional** — P1 picks a pre-recorded "Starting from pole. Well done." single clip; P2..POSITION_MAX composes "Qualifying put us to," + `position-number/<n>`; out-of-range or missing positions skip the clause entirely so the greeting + conditions still play. Snapshot is **plugin-composed** (telemetry-derived `RaceStartConditions` + PI driver-name pick), translator exposes `getRaceStartConditions()` without the pit speed limit (race start doesn't speak it). Family `race-start` (distinct from `session-start`) reserves the namespace for future race-only openers (wet-race brief, formation-lap brief) so a future addition can preempt this scenario without affecting practice / qualifying.
- **Race status + race end** — issue #569. Two families on the same trigger model: race-status (`lap.completed` `where:` filters on `sessionType === "race" && lapsSincePositionChange > 0 && lapsSincePositionChange % 3 === 0 && !getRaceFinishedFired()`) plus race-end (`race.finished`, a once-per-session latched event the translator emits on the first lap.completed after the checkered raises in a race). Demonstrates **payload extension as cadence anchor** — `lap.completed.lapsSincePositionChange` is computed in `diffLaps` from `state.lastPositionChangeLap`, anchored on the driver's first valid lap (no change yet) so a hold-position-from-start driver still hears every-3 status updates; the same field acts as both the cadence counter (race-status) and the change-detection signal (position-change). Race-end uses a **latch-once event** rather than a transient — the diff emits race.finished only on a successful position read so a missing-position lap defers the latch and the next lap.completed retries. Cross-scenario coordination via a **state-field resolver** (`isRaceFinished()`) lets race-status's `where:` suppress on the final lap without inter-scenario coupling — the diff publishes race.finished first into the pending queue, so by the time lap.completed's where: runs the latch reads true. Two new bus events (`position.changed` is added as plumbing for future per-change race callouts but has no consumer this issue; `race.finished` is consumed by race-end). Reach for this pattern when a single bus event needs to drive multiple families with different cadence/gating rules, and when one family's fire needs to suppress another's same-tick fire.
- **Overtake gained / lost** — issue #574. Two-event family with mirrored gain/loss semantics. **Bus event extension instead of a new shape** — `overtake.completed` was extended with position context (`position`, `previousPosition`, `gapBehindMeters`, `isLeader`, plus the class-position fields) rather than introducing a separate `overtake.gained` event, because the gain side already existed; the new `overtake.lost` parallels it for the loss direction. **Physical-gap gating in the translator** — on top of the existing `OVERTAKE_HOLD_MS = 3000` sustainment + `OVERTAKE_MAX_JUMP = 3` sim-glitch filter, a 10 m (`OVERTAKE_MIN_GAP_M`) distance check between the player and the relevant neighbour (whoever's now immediately behind for a gain; immediately ahead for a loss) postpones emission rather than dropping it — pending state is held until the gap opens up. Computed from `CarIdxLapDistPct` × parsed track length (cached on `TranslatorState.trackLengthMeters` keyed by `(TrackID, SessionNum)` so a track change invalidates the cache). When track length isn't parsed yet, the gate is treated as unknown and emission proceeds — don't punish missing data. **Driver-name composition from a dedicated per-name clip group** — the loss line's "Come on, &lt;name&gt;." slot is a full per-name clip from the `position-overtake-come-on` group (one entry per driver name + a `driver` fallback), resolved via `resolveActiveDriverName(driverNames, "driver")` so the line stays complete even when the user's name isn't recorded. Originally (#542/#574) this slot reused the `session-start-greeting/<driverName>.mp3` pool, but those clips carry an "Ok, &lt;name&gt;," greeting prefix, so the spliced line played "Come on, Ok, Niklas. Don't give up..." — #591 replaced the splice with purpose-recorded "Come on, &lt;name&gt;." clips and dropped the generic `come-on-01` from the sequence. **Two scenarios under a shared `family: "overtake"`** so a fast sequence of swaps doesn't stack stale callouts — the engine preempts the in-flight family-mate when a fresher fire arrives. Reach for this pattern when extending an existing event with new payload fields (vs. introducing a parallel event), when a gate needs to *postpone* rather than *drop* a pending fire while waiting for an external condition, and when a callout needs to compose with a per-user dynamic clip that already has a pool. The shared "We're currently P[n]" readout (live position at speak-time + cross-trigger cooldown) lives in `position-readout.ts` and is reused by overtake, lap-completed (#566), and race-status (#569) — see the `POSITION_CURRENTLY_CLIP` constant + `tryClaimPositionAnnouncement` cooldown.
- **Multi-class overtake detection** — issue #588. Demonstrates **keying change-detection on the position that's actually spoken**. The overtake diff originally detected on OVERALL position (`calculateRacePositions[playerCarIdx]`), but in a multi-class race the overall rank churns as other-class cars pass / pit / lap even while the driver holds station in class — so a steady class-leader heard an endless stream of "we lost a position / we're currently P[class]" (the readout already speaks CLASS position in multi-class via `selectLivePosition`). Fix: `diffOvertakes` computes `useClass = isMultiClass === true && rawClassPos > 0` and runs all change-detection / sustainment on `currentPos = useClass ? classPos : overallPos` against a `lastEffective` baseline; single-class is byte-identical (still overall). The payload keeps overall `position`/`previousPosition` and class `classPosition`/`previousClassPosition` (consumers read class in multi-class); `isLeader` stays overall ("leading the race" ≠ class P1). The physical-gap gate is skipped in multi-class — the class neighbour can't be found from the overall `positions` array, and a class change sustained for the hold window is already a real overtake. Reach for this pattern when a callout's *detection* signal and its *spoken* value must be the same projection of multi-dimensional telemetry (overall vs class), and when a gap/neighbour gate doesn't translate to the new projection (skip it rather than mis-compute).
- **Overtake round-trip suppression** — issue #597. Demonstrates **gating an emission on the last *announced* value, not just the per-tick baseline**. The overtake baseline (`lastPosition`/`lastClassPosition`) advances every tick, so a momentary unannounced excursion (P10 → P9 → P10, where the P9 leg never sustained the hold window) moved the baseline to 9 and the settle back to P10 confirmed as a *loss* (9→10) — the driver heard only the down-leg of a flicker. Fix: `state.lastCalledPosition` tracks the position last actually announced by a callout; at each confirmed gain/loss (after the sustainment + gap gates) the emission is skipped when `currentPos === state.lastCalledPosition` (net no-op since the last callout), and `lastCalledPosition` is updated *only* when a callout actually fires. Seeded on the first eligible tick, rolled silently under caution, reset in `resetOvertakeState`, and compared in the same effective space the detection uses (#588). A *sustained, announced* gain then a real loss back still fires both (the gain updated `lastCalledPosition`); only unannounced round-trips are suppressed. **Race-start grid seeding (#568 follow-up):** at a race start the first-tick seed anchors *both* `lastPosition` and `lastCalledPosition` to the player's announced starting grid slot — the translator passes `resolveStartingGridPosition(sessionInfo)` (the same overall qualifying position the race-start callout speaks) into `diffOvertakes` as an optional trailing `startingGridPosition` param (trailing + optional so the diff's ~50 call sites stay untouched). So early-race gain/loss is measured from the grid and an opening-lap round-trip back to it is silent. (Originally single-class only because the grid value was overall; #599 made `resolveStartingGridPosition` return the EFFECTIVE grid slot — class in multi-class — and the diff now seeds the matching detection space, so multi-class anchors to the class grid slot too.) Reach for this pattern when a transient that never produced output can still move a baseline and make the *return* to the last-spoken state look like a new event — track the last-emitted value and short-circuit equal returns; and when a baseline should start from a *semantically meaningful* anchor (the grid) rather than the first value the diff happens to observe.
- **Pit-box count-in** — issue #600. Demonstrates **a callout driven by a derived continuous distance rather than a sim enum/state diff**. The translator resolves the player's box from `SessionInfo.DriverInfo.DriverPitTrkPct` (`resolvePitBoxTrkPct`) and `diffPitBoxCountdown` computes the forward gap from `LapDistPct` to the box, folded around the lap, times the cached `state.trackLengthMeters` (reused from #574). The matching mark is the **band** the remaining distance falls into — the smallest threshold (120/100/80/60/40/20 m) still ≥ the gap, with a `1e-6` epsilon so a `pct × length` value a hair above an exact boundary still lands in the intended band — so a normal approach fires `five`→`pit-now` once each, and once the box is passed the gap folds to ~a full lap so no band matches and the count stops. **Threshold-crossing via entry-seeding (issue #601 review):** on the first valid tick of a pit-road visit the diff seeds every already-passed threshold into `state.pitBoxMarksSpoken` (gated by `state.pitBoxEntrySeeded`), so a mark only fires on a genuine downward crossing — joining pit road mid-band (e.g. at 70 m) never speaks a just-passed number ("three"); the count picks up at the next mark ahead. Both the set and the seeded flag are **cleared whenever the car is off pit road**, so a second stop counts down again; there's no connect-time seeding concern since the diff keys on live distance, not a transition. Gated on `OnPitRoad` and **suppressed in the stall** (`PlayerCarInPitStall`) so an arrived/connecting-in-box car gets no spurious "pit now". The audio side is **six per-mark scenarios → one `count-in` opt-in** (the track-conditions multi-scenario/single-subject shape), `family: "pit-box"` for preempt, and **no radio-open/close frame** (terse countdown). New bus event `pitBox.countdown` with a `{ mark: PitBoxMark }` payload; the box position is known up front so the count-in works on the **first** stop of a session — no learn-once needed. Reach for this pattern when the trigger is a continuous measurement (distance, time-to-X) rather than a discrete state change: band-select on the live value, dedup per-episode in a Set keyed to the activity window, and reset on the window's exit edge.
- **Invalid-lap prefix on the position callout** — issue #572. Demonstrates **extending an existing callout with a contextual prefix derived from a new payload field, without adding a new opt-in**. Adds `lapIsValid?: boolean` to the `lap.completed` payload — derived in `diffLaps` from `resolveLapIsValid(telemetry)` which `every`s the four `LapDeltaTo*_OK` flags (`LapDeltaToBestLap_OK`, `LapDeltaToSessionBestLap_OK`, `LapDeltaToSessionOptimalLap_OK`, `LapDeltaToSessionLastlLap_OK` — preserving iRacing's "Lastl" typo) and returns `undefined` when no flag is present so consumers fall through to the existing path on a missing signal. The position scenario wraps its existing `if/then/else` (pole vs intro+number) in a new outer `if/then/else` keyed on `sessionType === "qualifying" && lapIsValid === false`; the invalid branch is composed from static clip steps — a local `POSITION_DIDNT_COUNT_CLIP` ("That lap didn't count.") followed by the shared `POSITION_CURRENTLY_CLIP` (#574's "We're currently") and the existing `position.number` var, beating the pole / "better" branches unconditionally. **Gated to qualifying** so the #574 race path (live position at speak-time + shared cooldown) is untouched — "that lap didn't count" has no meaning in a race where every lap counts; the frozen-snapshot `position.number` resolver returns the qualifying number. No new opt-in key — rides on `calloutEnabledPositionChange` because the invalid prefix is conceptually the same callout with a contextual modifier; if user feedback later wants it independently silenceable, add `calloutEnabledPositionInvalidLap` as a separate subject. Reach for this pattern when the new behavior is a *modifier* of an existing callout (rather than a parallel subject) and the trigger is already on the bus payload.
- **Class focus in multi-class: starting grid + leader** — issue #599. Demonstrates **making the LAST overall-only references consistent with the class-aware detection (#588) and readout**. Two strands. (1) **Starting grid position.** `resolveStartingGridPosition(sessionInfo)` returns the EFFECTIVE grid slot — the player's CLASS slot in multi-class, overall otherwise. The class slot isn't a direct YAML field, so it's computed: build a `CarIdx → CarClassID` map from `DriverInfo.Drivers`, then count same-class `QualifyResultsInfo.Results` entries with a better (lower) overall `Position` and `+1` (falls back to the overall slot when the player's class can't be resolved). One function feeds BOTH consumers — the race-start callout (`getRaceStartConditions().playerCarPosition`, so "Qualifying put us to P3" / "Starting from pole" now mean the class slot) and the overtake baseline seed — so they stay aligned by construction. `diffOvertakes` now applies the grid seed in the **matching detection space**: the class baseline in multi-class (where detection runs on class position, #588), the overall baseline in single-class. The transient multi-class tick before `PlayerCarClassPosition` populates (`rawClassPos === 0`, so `useClass` is false) detects in overall space while the grid value is a class slot — a space mismatch, so it falls back to the live seed rather than mis-anchor. (2) **Leader wording.** The overtake payload's `isLeader` field stays OVERALL-only (`position === 1`), but the *spoken* leader is now the EFFECTIVE leader via `isOvertakeEffectiveLeader(data)` (= class P1 in multi-class, overall P1 otherwise) — shared from `position-readout.ts` (the lower-level module, so `overtake.ts` imports it without a cycle) by both the overtake reaction (picks the "leading our class" clip vs the "leading the race" clip on `isMultiClass`) and the gained position readout (suppresses the follow-up when effective-leader, since the reaction states the position). The race-status "still leading" line was already triggering on class P1 in multi-class (it keys on `selectLivePosition`), so #599 only fixed its **wording** — its `stillLeading` var picks `still-leading-class-01` when the live position is multi-class. Two new clips (`position-overtake/nice-pass-leader-class-01`, `race-status/still-leading-class-01`) in `default.voice.json`; no new opt-in (the leader wording rides on the existing overtake/race-status callouts) and no bus-payload change (consumers derive effective-leader from the existing `position`/`classPosition`/`isMultiClass` fields). Reach for this pattern when a family is *mostly* class-aware and the remaining overall references are an inconsistency: derive the projection (class vs overall) at the consumer from fields already on the payload rather than adding parallel events, and place a shared helper in the dependency sink to avoid an import cycle.
- **Replay-only session gate — suppress callouts while the user is watching a replay, without regressing the #568 live-transition path** — issue #604. Demonstrates **picking the right discriminator from session YAML when a transient telemetry flag isn't safe to gate on**. Two emissions in `translator.ts` (`handleTick`) are deliberately published BEFORE the main replay guard at ~L841 so they survive the brief `IsReplayPlaying: true` tick that a live qualifying → race transition passes through: the SessionNum-delta `session.changed` (~L811) and the fresh-connect synthetic `session.changed { from: -1, to: N }` (~L754). Both fire `session.changed`, which is the trigger for race-start (`packages/audio-scenarios/src/catalog/pit-crew/race-start.ts`). Bug surface: a saved-replay scrub across a session boundary, or a plugin connect while the Replays UI is open, would synthesize a race-start brief for a race the driver isn't in. **Cannot gate on `IsReplayPlaying`** — that's the exact transient signal #568 was placed pre-guard to bypass. **Heuristic-based detection doesn't work either:** a layered "never-live + sustained-replay-ticks" approach needs threshold tuning the live-transition window doesn't permit, and `ResultsPositions`/`LapsComplete` presence in the current session can't distinguish "live race just-loaded" from "replay jumped to start of race" (in both, the current session reads zero laps; only later sessions' data differs, and during a live qualifying → race transition the FROZEN qualifying session ALSO has populated `LapsComplete`). **Validated against a real telemetry snapshot (`local/telemetry-snapshot-20260528-203804.json`):** iRacing exposes `WeekendInfo.SimMode` — `"full"` while live, `"replay"` while in the Replays UI — and crucially, SimMode does NOT flip during the live qual → race transition's `IsReplayPlaying` flicker; it stays `"full"` the whole time. `isReplayOnlySession(sessionInfo)` is a one-line read on this field. Gates **both** pre-guard emissions on `!isReplayOnlySession`. **Freezing `lastObservedSessionNum` during replay-only is load-bearing**: if the tracker advanced through a replay scrub, exiting back to live with a different SessionNum would emit a phantom `session.changed` measured against the scrubbed value. With the tracker frozen at the last live-observed SessionNum, exit-to-live either matches (no emit) or detects a genuine intervening session change. The fresh-connect synthesis is also gated **without latching** `freshConnectFireChecked`, so a user who closes the replay UI and starts a real session still gets the synthesis on the first live-SimMode tick. Reach for this pattern when a callout-driving signal must remain pre-guard but the SAME signal will mis-fire under a parallel use case, and look in session YAML for a self-reported discriminator before designing a tick-counter or persistence heuristic — iRacing often exposes the distinction directly (here: `SimMode`).
- **Self-managed running order — freeze blipped / teleported / retired cars, quiet retirement gains, tune reaction frequency** — issue #603. Demonstrates **owning the running-order state instead of re-deriving it from raw telemetry every tick**, classifying a callout by the cause of the position change, and gating audio behaviour on the cause without a new bus event or opt-in. (1) **The root issue is the same shape across three symptoms.** iRacing snaps a car's `CarIdxLapCompleted` / `CarIdxLapDistPct` / `CarIdxTrackSurface` to `-1` / `NotInWorld` the moment the car is gone (proven from dump-file analysis: a car a lap ahead went `lc6 dp0.142 ts3` → `lc-1 dp-1 ts-1` in one 60 Hz tick during a connection blip), and a tow teleports `CarIdxLapDistPct` to the pit. `calculateRacePositions` (pure, `@iracedeck/iracing-sdk`) ranks only cars with `lc >= 0 && dp >= 0`, so re-deriving order from raw telemetry every tick reshuffles the player on every blink / tow / vanish — manifesting as a string of phantom "Nice pass" gains (finishers leaving), single-tick rank churn (mid-race blinks of cars a lap ahead), and the "tow looks like a pass" case. Anchoring on iRacing's official `CarIdxPosition` is not an option — it only refreshes at S/F. (2) **One mechanism replaces the per-symptom patches.** `updatePositionTracking` (`diff/race-finish.ts`) maintains `state.positionLastKnownScores[carIdx]` (the last in-world `lc + dp`) and `state.positionFrozen: Set<number>` per car. A car's **effective score** is its live `lc + dp` while it's moving normally on track, **frozen at its last-known good value** whenever its telemetry is invalid (`NotInWorld`) or its `lc + dp` jumps discontinuously from the previous tick by more than `TELEPORT_THRESHOLD` (0.05 lap — well above the ~0.0003 lap/tick of racing motion and the ~0.002 score-jump on a natural S/F crossing, well below any plausible tow). A car unfreezes when it resumes continuous forward motion (issue #697 refined this — the original "live score returns to within the threshold of the anchor" rule pinned a towed car at its stale anchor for the rest of the race; see the #697 entry below). `calculateFrozenRacePositions` is the stateful wrapper around the pure SDK helper, sorting everyone by their effective score with the identical comparator. **Same rule, all four symptoms:** blink → frozen for one tick, unfreezes on return; disconnect → frozen at last → player passes it only by *advancing past its anchor*; finisher into the garage → frozen at the finishing score → can't be passed without finishing; tow → live score jumps far from anchor → frozen at the pre-tow on-track score → held until it drives off again (#697). **Subsumes** an earlier checkered-detection design (`updateFinishedCars`, `USE_PER_CAR_CHECKERED`, `finishedCarScores`) AND a separate `−isTowed` patch — the unified model is more accurate and has fewer moving parts than either. (3) **Layering.** The wrapper lives in `sim-events-iracing` (it needs `TranslatorState`) while the SDK function stays pure (threading translator state into `iracing-sdk` would invert layering). Both `diffOvertakes` (detection) and `getLivePosition` (the spoken overall readout) consume it; `diffOvertakes` takes the frozen vector as a trailing optional `frozenPositions` param so existing call sites / tests that just want raw active positions stay unchanged. (4) **Retirement classification keys off the FROZEN ordering, not the raw active one** (CodeRabbit #605 review). When the player's effective rank improves, `isRetirementGain(state.lastFrozenPositions, playerCarIdx, state.positionFrozen, state.positionJustReleased)` returns true iff at least one car ranked ahead of the player in the PREVIOUS tick's frozen positions is currently in `positionFrozen` (or was just released from it this tick — `positionJustReleased`, #697 — so a tow-released rival dropping back doesn't read as a real pass). Critically — the moment of detection is the tick the player CROSSES a frozen car's anchor (which is when the rank actually changes in the frozen path), NOT the tick the car vanished. An earlier draft keyed off the raw active ordering and missed pure frozen-anchor gains, because by the time the player crossed the anchor the raw order had long since dropped the vanished car. Latched across the hold window so a real pass deepened by a same-window retirement ahead is caught (the latch re-evaluates every held tick). Emitted as the new optional `overtake.completed.fromRetirement`; the reaction `where:` returns false on `fromRetirement` (readout only, no "Nice pass"). Single-class only — multi-class detects on `PlayerCarClassPosition`, which iRacing recomputes correctly when a car leaves. (5) **Sparser reactions + podium.** `shouldReactToOvertake(effectivePosition)` replaces the old 20 s `tryClaimReaction` cooldown: P1/P2/P3 always react (each with a dedicated line — P2/P3 added alongside the existing P1 leader line, single + "…in class" variants), everything else rolls `REACTION_CHANCE` (~1/3, via an injectable `reactionRandom` test seam). Podium gains suppress the follow-up position readout (their line states the position). (6) **Bare/full intro.** `shouldSpeakIntro` drops the "We're currently" intro for a bare "P[n]" when it was spoken within `INTRO_COOLDOWN_MS` (30 s) AND the move is ≤1 position; a >1 jump always restores the full intro. Tracked in module state (`lastIntroAt` / `lastSpokenPosition`) and scoped to the overtake position readout (the shared `readoutSequence`); race-status / position-change keep their own always-full intro, since the existing 20 s cross-trigger position cooldown already prevents their double-announce. Reach for this self-managed-state pattern whenever a pure helper's output is missing cross-tick memory that several symptoms all want (don't add per-symptom patches — own the state once); and for the cause-classification pattern when the *same* position change should produce different audio depending on whether it was earned (a pass) or inherited (a retirement / frozen-anchor cross). Key on the PREVIOUS-tick effective ordering, not the raw one — the gain is detected on the anchor-crossing tick, and the raw order no longer remembers the frozen car was ever ahead.
- **Track-type-conditional emission — fire the pit-entry readback off the right edge per track type** — issue #634. Demonstrates **reusing an existing bus event but branching the *condition under which the translator's diff emits it* on a track-type classification**. The pit-entry readback rides the existing `pitLane.approaching` event; no new event, no new opt-in. On a **dirt oval** iRacing usually teleports the car straight into the pit stall, bypassing the approach zone, so the approach-zone trigger (`TrkLoc.AproachingPits`) never fires there — instead the diff keys on the `OnPitRoad` false→true **drive-in** edge, guarding out the teleport-to-stall case via `PlayerCarInPitStall` / `PlayerTrackSurface` so a tow/teleport directly into the box stays silent. On **all other track types** (road course, unknown, anything not special-cased) the approach-zone edge is unchanged. The exit direction never fires it (road course: approach-zone-on-exit suppressed; dirt oval: the `OnPitRoad` on→off exit edge doesn't trigger). No time delay (an earlier proposed 1.5 s hold was dropped). The classification is a local `TrackType` enum + `resolveTrackType` in `sim-events-iracing` derived from the `WeekendInfo.TrackType` session-YAML string — precedent: #604's `isReplayOnlySession` session-YAML read (look in session YAML for a self-reported discriminator). The enum was deliberately kept **local** to `sim-events-iracing` (not placed on the event-bus) because it rides in no event payload and has a single consumer; a follow-up issue tracks extracting a shared sim-agnostic domain-types home later. Reach for this pattern when the *same* callout should fire off a different telemetry edge depending on a track/session classification — branch the diff's emit condition on a classifier resolved from session YAML, and keep the classifier local until a second consumer or a payload need justifies promoting it.
- **Re-entry cooldown on the approaching-pits callout — debounce a transition edge that the same physical approach can bounce** — issue #650 (extends #634). Demonstrates **guarding a re-fireable emission edge with a time window so an accidental bounce of the trigger doesn't re-announce**. The `pitLane.approaching` callout re-fired when the driver left and re-entered the pit-entry trigger within a short span — on dirt ovals it's easy to drive out of pit road by accident and straight back in, hearing "approaching pits" twice. Fix: a 10 s cooldown (`PIT_APPROACH_COOLDOWN_MS`, defined in `diff/pit-lane.ts`, re-exported from the package index alongside `OVERTAKE_HOLD_MS`) gating BOTH emission branches via a local `fireApproach()` helper — the emit is skipped while `now < state.pitApproachCooldownUntil` and each real fire re-arms `pitApproachCooldownUntil = now + COOLDOWN`. This is the same **time-guard / `<x>CooldownUntil` timestamp** pattern as `pitActionCooldownUntil` (#476) / `speedingWarnedAt`: the `now` is threaded from `handleTick` into `diffPitLane(state, telemetry, trackType, now, emit)` exactly like `diffLimiter` / `diffToggles`, and the field lives on `TranslatorState` (type **and** `createInitialState`, kept in sync). **Scope is all track types** (not just the dirt-oval path the bug reproduced on) because the road-course approach-zone path can re-fire on a drive-out-and-re-approach too — one guard at the shared `fireApproach()` covers both #634 emission edges. Crucially the cooldown gates **only the audio emit**, not the road-course `approachAlertFired` rearm-until-back-on-track state machine: that branch still sets `approachAlertFired = true` whether or not the emit fires, so the existing suppression-until-back-on-track bookkeeping is untouched. Tested via a dedicated `diff/pit-lane.test.ts` that calls the diff directly with explicit `now` values (the only way to advance simulated time across the 10 s window — `translator.test.ts` reads the real `Date.now()` and can't). Reach for this pattern when a callout rides a discrete transition edge that the *same* real-world episode can re-cross within seconds (drive out and back in, a flag flickering between sibling variants): add a `<x>CooldownUntil` timestamp on `TranslatorState`, thread the existing `now`, gate the emit through a single helper so every emission path shares the window, and keep the cooldown orthogonal to any other state machine the branch maintains.
- **Release a frozen car on resumed motion, not anchor-proximity — fix the stuck position after a tow** — issue #697 (refines #603). The #603 unfreeze rule released a frozen car only when its live score returned to within `TELEPORT_THRESHOLD` of its anchor. A tow drops a car far from where it vanished, and as it races on its score only drifts *further* from the now-stale anchor, so that gate could never re-open — the car (and, after the player's own tow, the player themselves) stayed pinned at a dead position for the rest of the race. `getLivePosition` / the Session Info position display / overtake detection all read the same `calculateFrozenRacePositions` order, so the spoken position and the on-key display both read stuck (too optimistic or pessimistic depending on where the anchor sat relative to the live field). Fix in `updatePositionTracking`: keep the freeze trigger (a teleport — `NotInWorld`, or a discontinuous one-tick `lc + dp` jump) but decide **release from per-tick motion** — a frozen car is let go the moment it shows continuous forward motion (`score > prev` within `TELEPORT_THRESHOLD`), **wherever it is** (no track-surface gate — release on motion, not location), rolling its position back to live; a car that merely *stops* on track (no teleport) is never frozen, so it correctly loses places to cars still racing. A new per-car `state.positionPrevScore` carries the previous in-world score for the tick-to-tick delta; nothing downstream changes because every consumer already reads the same frozen order. Reach for this pattern when a "hold the last-known value through a glitch" state machine is released by comparing against a *reference that itself goes stale*: gate the release on resumed normal behaviour (continuous motion) measured tick-to-tick, not on proximity to the frozen reference.
- **Pre-green SessionState gate on the overtake family + qualifying-grid display — no phantom position callouts and no P0 before the green flag** — issue #647. Demonstrates **a shared pre-green predicate driving two consumers, and choosing the right grid-position SOURCE for a rolling start**. (1) **Callouts (part 1).** The overtake diff gated on race-session, pit-road, and caution flags but NOT on `SessionState`, so the grid / formation / parade-lap shuffle — which isn't a caution — leaked "Nice pass" / "we lost a position" callouts before the green flag. Fix: extend the existing caution branch from `if (underCaution)` to `if (underCaution || preGreen)` — that branch already rolls EVERY baseline (`lastPosition` / `lastClassPosition` / `lastCalledPosition` / pendings / `lastFrozenPositions`) silently and `return`s, and crucially runs BEFORE the `if (!state.overtakeInitialized)` seed, so staying pre-green leaves `overtakeInitialized` false and the first racing tick after green seeds cleanly from the settled green-flag order (no phantom emit at green). (2) **Shared predicate.** `preGreen` is `isPreGreen(telemetry)` — a pure helper in `@iracedeck/iracing-sdk`'s `telemetry-features.ts` (next to `hasPitLimiter`/`hasVisor`/`hasWipers`) returning true for the EXPLICIT pre-racing set (`Invalid` / `GetInCar` / `Warmup` / `ParadeLaps`), NOT `sessionState !== SessionState.Racing`. The explicit set gives two load-bearing properties: a missing `SessionState` matches none → `false` → unchanged behaviour for callers/tests that don't supply it, and `Checkered` / `CoolDown` stay live so late-race passes aren't suppressed. Shared by BOTH the overtake gate and the Session Info position display so suppression and the displayed position agree on when racing has started. (NOTE: distinct from the translator's fresh-connect race-start gate and `pit-readback`'s `inGridSessionState`, which deliberately use a 3-state set excluding `Invalid` — `Invalid` means "telemetry still settling, keep waiting" there, #604; the helper documents that these are intentionally separate.) (3) **Display SOURCE matters on a rolling start (part 2).** The Session Info position mode must show the qualifying position pre-green — but neither telemetry position source works during a rolling-start formation lap: validated against real telemetry (`local/telemetry-snapshot-20260604-21*.json`), `PlayerCarPosition` / `CarIdxPosition` read `0` for the WHOLE field until cars cross start/finish (iRacing scores positions only at the line), and `calculateRacePositions` excludes every car because `CarIdxLapCompleted` is `-1` on the formation lap (the `lapCompleted >= 0` active-car gate). The only pre-scoring source is the qualifying grid in session YAML — exposed as `getStartingGridPosition()` (overall + class slots) from `sim-events-iracing`, wrapping the same `resolveStartingGridSlots` the race-start callout uses (`QualifyResultsInfo.Results[player].Position`, populated the moment the grid is set, for standing AND rolling starts). The display reads it not just pre-green but through the **green-flag run to the line** — until the player crosses S/F to start racing (`LapCompleted` flips from the `-1` out-lap sentinel to `>= 0`), because right after the green the live order churns (the field is bunched and cars cross S/F one-by-one, so the `lapCompleted >= 0` ranking is computed over an incomplete, shuffling subset → a P13 grid car flickered P1→P4→P13). It falls back to live-standings telemetry only when the grid can't be resolved. NOTE the two consumers intentionally use **different** "racing has started" signals: the callouts gate on `SessionState === Racing` (#647's stated spec — start calling the moment the green flies), while the display holds the grid one step longer, until the player's first S/F crossing, because a churning displayed number is worse than a stable-but-slightly-stale one. Reach for this pattern when (a) an existing family leaks callouts during a non-racing phase no flag gate covers — fold a shared `isPreGreen` into its silent-baseline-roll branch, never a `!== Racing` negation; and (b) you need a position "before the green flag" — read the qualifying grid from session YAML, NOT `PlayerCarPosition` or the lap-distance order, both of which are empty/zero through the formation lap AND the bunched green-flag start.
- **Capability-gated callout — suppress pit-limiter callouts on cars that have no pit limiter** — issue #639. Demonstrates **gating a callout on a CAR-CAPABILITY flag derived from the PRESENCE of a `dc*` driver-control field**, so the Race Engineer stays silent where the callout is physically meaningless. A large chunk of the iRacing roster has no pit limiter, where "don't forget your limiter", "limiter still on / dropped", and pit-speeding warnings are nonsense — so all of these are gated on `hasPitLimiter` (added in #637, exported from `@iracedeck/iracing-sdk`, true when telemetry exposes the `dcPitSpeedLimiterToggle` driver-control field, i.e. the car HAS a limiter — distinct from "limiter currently engaged"). **Two gating shapes by composition source.** (1) The four `pit-limiter.ts` scenarios (`carControl.limiterToggled`, `limiter.missing`, `limiter.dropped`, `limiter.speeding`) each fire off a single event, so each carries an `hasPitLimiter(e.telemetry as TelemetryData | null)` guard in its `where:` predicate — read live at event-arrival, consistent with the live-gating convention (#467's per-event reads) so it reflects the car the user is currently in rather than a re-registration on settings change. (2) The pit readback's "Don't forget your limiter." pre-opener slot composes from a snapshot rather than a single event, so its `if:` step has no raw telemetry to read — a precomputed `hasPitLimiter: boolean` was added to `PitReadbackSnapshot` (computed in `buildSnapshot` in `sim-events-iracing/src/diff/pit-readback.ts` via `hasPitLimiter(telemetry)`) and the slot's `if:` gates on it. This is the exact same shape as the existing `hasDamage`-gated fast-repair slot (#489). The slot is **suppressed entirely** when the capability is absent — no "off"/alternative line. Depends on #637, which introduced the `hasPitLimiter` / `hasVisor` / `hasWipers` capability helpers (each derived from the presence of its `dc*` driver-control field). Reach for this pattern when a callout (or readback slot) is meaningless on cars lacking a particular control: gate on a car-capability flag derived from the presence of the matching `dc*` field — read it live in `where:` for event-driven scenarios, or carry it as a precomputed boolean on the readback snapshot (like `hasDamage`) for snapshot-composed slots — and suppress the slot entirely rather than substituting an alternative line.
- **Setup-name mismatch warning — append a conditional clause to existing intros, gated on a live user-editable regex** — issue #625. Demonstrates **adding a callout as a conditional clause inside existing scenarios rather than a new scenario/event**, and **driving its trigger from a user-editable regex tested against a session-YAML string at fire time**. The warning ("Our setup name suggests that we're on a race/qualifying setup. Please double-check.") is appended before `@pit-crew.radio-close` in both `session-start.ts` (gated `getSnapshot()?.sessionType === "qualifying"`) and `race-start.ts` (race-only by construction) as an `{ if, then: [clip] }` step — no new bus event, no new `SCENARIO_ID_TO_*` entry, and the opt-in is checked **inside** the `if` predicate (not via `wrapCalloutScenario`, since a clause inside an existing scenario can't be wrapped independently). The match logic lives in `@iracedeck/deck-core` as pure, testable functions (`evaluateSetupWarning` / `setupNameMatchesPattern` / `compileSetupWarningPattern`), kept acyclic by splitting the default-pattern constants into a leaf `setup-warning-constants.ts` (so `global-settings.ts` reads the Zod `.default()`s without importing the `pi-warnings`-dependent matcher module). The setup name is read via a new translator accessor `getDriverSetupName()` (`DriverInfo.DriverSetupName`) — same session-YAML-read precedent as #604's `isReplayOnlySession` / #634's `resolveTrackType`. **Plugin-composed resolver**: each plugin passes `registerPitCrew` a `(kind) => evaluateSetupWarning(kind, getGlobalSettings(), getDriverSetupName())` closure (live-read at fire time, like every other per-callout gate), placed just before the master gate so the master stays last. **Two configurable patterns** (`setupWarningQualifyingPattern` / `setupWarningRacePattern`) are string global settings with their defaults in the Zod schema + the PI `default` attribute; a broken regex is compiled defensively (try/catch → `null` → callout skipped) and surfaced via the `_warnings` banner (`validateSetupWarningPatterns`, run on every `onGlobalSettingsChange`), with a PI **Reset** button per field that restores the field's own template `default` via `.save()` (no duplicated default). Note: the default patterns' boundary class is `[ ._-]` (space, period, underscore, literal hyphen with `-` kept last) — `(^|[ ._-])…([ ._-]|$)` — so hyphen- and underscore-separated names (`quali-fast`, `race-trim`, `VRS_race_v2`) all match. Hyphen was an examples-authoritative choice that deviates from the issue prose's "hyphens not matched"; underscore was added afterward because it's a common iRacing setup-name separator. Reach for this pattern when the new behavior is a *clause* appended to an existing intro (not a parallel subject), and when the trigger is a user-tunable string match against session YAML rather than a telemetry diff — keep the matcher pure in `deck-core`, gate the opt-in inside the clause's `if`, and banner an invalid user pattern rather than letting it throw.
- **Weighted interpreter scheduling — replace the priority/preempt enum with a tunable weight + interrupt/queueable + an exclusive-focus weight floor** — issue #652. Demonstrates **making scheduling importance a continuous tunable number instead of a fixed four-level enum**, and **giving an owner a way to hold a bus exclusively**. The old `Scenario` fields `priority: "low" | "normal" | "high" | "urgent"` and `preempt?: boolean` are gone. Replaced by: `weight?: number` (higher wins a busy bus; named bands exported from `dsl.ts` as `WEIGHT` — `TRANSIENT = 5`, `CHATTER = 10`, `NORMAL = 50` (the default when omitted), `SAFETY = 70`, `CRITICAL = 100`; any integer allowed); `interrupt?: boolean` (default false — when a fire wins a busy bus over an in-flight LOWER-weight fire, `true` cuts it mid-sentence, `false` waits for the current line to finish; equal/lower-weight fires never cut); `queueable?: boolean` (default false — when a fire can't take the bus now (equal/lower weight, or below a focus floor), `true` defers it for replay when the bus next idles, `false` drops it); and `focusOwner?: string`. **Same-`family` preemption is unchanged** — a newer same-family fire still replaces the in-flight family-mate wholesale, regardless of weight, and is NOT stashed for replay (distinct from the cross-family weight contest). **Exclusive focus (new):** `IScenarioEngine.acquireFocus(bus, ownerId, floorWeight)` / `releaseFocus(bus, ownerId)` raise a per-bus weight floor; while held, only fires with `weight` at or above the floor — or the owner's own (`focusOwner === ownerId`) — play, everything else defers (if queueable) or drops, and releasing drains any deferred fire. The single per-bus pending slot holds the **highest-weight waiting fire** (ties → newest), replayed **unconditionally** — its `where:` is NOT re-run, since a side-effecting predicate (the position-readout cooldown claim) would fail on a second call; freshness comes from var resolvers reading live state at speak time. **Catalog migration (canonical examples):** flag callouts moved `priority: "normal"` → `weight: WEIGHT.SAFETY` (above routine chatter and a spotter focus floor), the meatball stays the cut-through line at `weight: WEIGHT.CRITICAL` + `interrupt: true`; background commentary (pit-readback, service-reminder, position, position-readout, race-status, race-end) moved `priority: "low"` → `weight: WEIGHT.CHATTER` + `queueable: true`; pit-lane phase announcements (pit-approach, pit-exit, stall-departure) `priority: "high"` → `weight: WEIGHT.SAFETY`; fuel-critical (3/1/0 laps) → `weight: WEIGHT.CRITICAL` + `interrupt: true` while the advisory 5-lap fuel line stays default `WEIGHT.NORMAL`; ordinary `priority: "normal"` callouts now simply omit `weight`. Reach for this model whenever a fixed importance enum proves too coarse — make importance an integer with named bands so a new callout can slot *between* existing levels without an enum migration, split the "what happens when I can't play now" decision into two orthogonal booleans (cut-vs-wait via `interrupt`, defer-vs-drop via `queueable`), and expose `acquireFocus`/`releaseFocus` when a feature (e.g. a spotter) needs to claim a bus exclusively above a weight floor rather than racing every other producer per-fire.
- **Interpreter-scheduled imperative engine + exclusive-focus side-by-side gate — spotter proximity callouts** — issue #651. Demonstrates **a second imperative engine that, unlike radar-engine, schedules through the interpreter rather than playing direct**, **an exclusive-focus floor held for the duration of a condition (a car alongside) rather than per-fire**, and **a one-clip-per-transition state machine that reuses an existing bus event**. `spotter-engine.ts` (mirroring `radar-engine.ts`) owns a module-level state machine over `RadarState` (`clear`/`left`/`right`/`both`/`two-left`/`two-right`) driven off the **existing `radar.changed` event** — no new bus event, no translator diff, no new scenario-harness shortcut (the existing radar shortcuts already emit `radar.changed`). **Interpreter-scheduled, not direct.** Where radar-engine plays ticks straight on `AudioChannel.Radar`, spotter-engine computes a clip path into a module var per transition and fires a single var-driven scenario — `getScenarioEngine().fire("pit-crew.spotter-call")` whose lone step is `{ var: "spotterClip" }`, registered via `defineVar("spotterClip", () => pendingSpotterClip)`. **All clip selection is var-driven (no new pools)** so the scenario carries no audio-manifest dependency at load time and build/test stay green before the clips are generated; the engine's two no-repeat arrays (clear pool, still-there pool) live as module consts inside the engine, not in `pools.ts`, for the same reason. **One clip per transition** — each callout (arrival / escalation "Two cars left." / de-escalation "One car left." / combined-swap "Clear right. Car left." / "Three wide." / "Clear.") is a single pre-recorded clip; the engine never sequences multiple clips into one callout, with de-escalation/swap wording baked into single combined clips. **Exclusive-focus side-by-side gate** — `acquireFocus(AudioBus.Voice, "spotter", WEIGHT.SAFETY)` is held while ANY car is alongside (acquired on the first non-clear transition, NOT re-acquired on subsequent non-clear transitions, released on `→clear` or force-clear), so routine chatter is held back while safety flags at/above the floor still break through; the scenario sets `focusOwner: "spotter"` so the spotter's own calls bypass its own floor. This is **condition-scoped focus** (held across many fires for as long as the gate condition holds) — distinct from the per-fire weight contest. **Road/oval terminology** is resolved per fire via `resolveTrackDirection` / `getTrackDirection` (`@iracedeck/sim-events-iracing`, a `WeekendInfo.TrackDirection` session-YAML read — same precedent as #634's `resolveTrackType` / #604's `isReplayOnlySession`): road courses speak left/right, ovals inside/outside, with a single clip catalog covering both. **No standalone master — Race Engineer master + two live opt-ins.** The spotter is a Race Engineer voice callout family (like flags / position / lap-time), NOT a separate mode or button: there is no `pitCrewSpotterEnabled` toggle. The whole engine is gated by the existing Race Engineer master (`pitCrewRaceEngineerEnabled`, the "Race Engineer" toggle button) plus two per-callout opt-ins, both **default ON** — `calloutEnabledSpotterCars` (transition calls) and `calloutEnabledSpotterStillThere` (the ~4 s reminder loop) — read live on every event/tick. The focus gate + loop keep operating when only one opt-in is off, but the engine goes fully inactive (no focus floor, no fire) when the master is off or **both** opt-ins are off. Reach for this pattern when a feature needs a stateful imperative driver over an existing proximity/state event but should still route through the interpreter's weight/focus model (rather than playing direct like radar) — keep the clip selection var-driven to avoid a manifest dependency, hold an `acquireFocus`/`releaseFocus` floor across the *duration of a condition* (not per fire), gate it under the existing Race Engineer master + per-callout opt-ins (no new standalone master), and resolve any track-shape terminology from session YAML at fire time.
- **Start-light family (window-gated countdown), green-suppression-at-start, and the caution-waving split** — issue #480. Demonstrates three reusable patterns for the flags iRacing exposes but the engineer ignored. (1) **Window-gated `SessionTimeRemain` start countdown with an AI guard.** A new stateful diff (`diff/start-lights.ts`) drives the standing-start numeric countdown ("Sixty." … "Five.") off `SessionTimeRemain` inside the standing pre-start window — originally `StandingStart ∧ SessionState===Warmup ∧ StartReady set ∧ SessionTimeRemain>0`, **widened by #666** to `StandingStart ∧ SessionState ∈ {GetInCar, Warmup} ∧ ¬(StartSet∨StartGo) ∧ SessionTimeRemain>0` once `SessionTimeRemain` was confirmed to be the real time-to-lights from `GetInCar` onward (the `StartReady` proxy and the "bogus get-in buffer" concern proved unnecessary; the window now opens at `GetInCar` and closes when the gantry `StartSet`/`StartGo` light). On the first in-window tick the diff **seeds a ceiling** from that first trustworthy reading (`startCountdownCeiling = SessionTimeRemain`, the highest eligible threshold) so a procedure that begins below 60 s never blurts "Sixty."; on each tick the candidate thresholds are `T ∈ {60,30,10}` with `T ≤ ceiling ∧ SessionTimeRemain ≤ T ∧ ¬fired(T)`, and the diff marks all candidates fired but **emits only the smallest** crossed threshold — so a dropped tick produces the most-recent number, never a stale burst. A single `startLight.countdown.raised { seconds }` event feeds the per-number scenarios (`where: seconds===N`) under one `calloutEnabledStartCountdown` opt-in (the pit-box `pitBox.countdown { mark }` shape) — five marks originally, trimmed to three (60/30/10) by #666. An **AI guard** (`resolveIsAiRace`) originally suppressed the numbers entirely as a belt-and-suspenders over the window-gate, but **#666 removed it** — the ceiling-seed already keeps a compressed pre-start window from speaking a threshold it can't reach, so AI races now get the countdown too. The gantry lines fire off plain `SessionFlags` rising edges (`StartSet`/`StartGo` vs `state.lastStartLightBits`; the original standing-only `start-ready` line was dropped by #666, so rolling's lead-in is the `one-pace-lap-to-go` / `green-held` flags). Countdown state resets on window exit (`StartGo`, `→Racing`, session change). Two grouped opt-ins (`calloutEnabledStartLights` for the gantry lines, `calloutEnabledStartCountdown` for the countdown numbers) mirror the pit-box "many scenarios → one subject opt-in" precedent. (2) **Green-suppression-at-race-start.** When the green transition coincides with `StartGo` being set, `diff/flags.ts` **skips** `flag.green.raised` so the start-light family alone owns the start (no "Go go go" + "green flag" double-talk); the suppression is stateless, so restarts (caution → green, no `StartGo`) keep firing green normally. (3) **Caution-waving split with an escalation-safe `yellow.cleared`.** `YellowWaving` and `CautionWaving` become their own more-urgent callouts (the static `flag.yellow.raised {scope}` scenarios are untouched), and the cleared check is reworked to fire **only when ALL yellow-ish bits** (`Yellow｜YellowWaving｜Caution｜CautionWaving`) clear after at least one was set — tracked via a new `state.lastAnyYellow` boolean — so a static→waving escalation (which clears the static bit while setting the waving one) never mis-fires "all clear". `Disqualify` is also split out of the generic `black` callout into its own "Disqualified. Pull off." line, with `Furled` and `DqScoringInvalid` as new simple rising-edge bits. Reach for the window-gate-plus-ceiling-seed pattern when a continuous telemetry value is only trustworthy inside a narrow state window and must emit one threshold per crossing (seed the ceiling from the first in-window reading, emit the smallest crossed, mark all fired); for green-suppression when one family must cede an event to another at a specific moment (gate the diff's emit on the co-occurring flag bit, keep it stateless so the other case is unaffected); and for the all-clear-on-every-bit pattern when a "cleared" callout must survive an escalation between sibling variants (track a single "any of this group is set" boolean, fire cleared only on its falling edge). (4) **Race-only gating on a `where:` predicate (post-merge fix).** iRacing raises the race-grid bits (`OneLapToGreen` etc.) while forming the race grid at the END of a qualifying session — so "One pace lap to go." fired at the qualifying checkered. The race-formation / progression scenarios (`crossed`, `one-lap-to-green`, `green-held`, `ten-to-go`, `five-to-go`) and the whole start-light family gate their `where:` on `isRaceSession(getSessionType())` (the helper race-start already exposes) — live-read at fire time, the same session-awareness the green/white/checkered scenarios use. Black flags and the caution-waving variants stay session-agnostic (they legitimately occur in qualifying). The gate is on the SCENARIO, not the diff, so the event still emits (harness-testable) but only speaks in a race; safe for the critical `start-go` because at a real race start the session is unambiguously "Race". Reach for a scenario-layer `where:` session gate (rather than a diff gate) when a flag bit is physically real but only *meaningful* in one session type, and the bit can leak across the qual→race grid-formation boundary. (5) **In-car + standing-start gates on the same `where:` (second post-merge fix).** Two more leaks via the same scenario-`where:` mechanism: the callouts fired while the user was OUT of the car (on the grid / spectating — `IsReplayPlaying` is suppressed by the translator's replay guard, but out-of-car-but-live is not), and "One pace lap to go." fired at a STANDING start (iRacing sets `OneLapToGreen` throughout the standing grid, but a standing start has no pace lap — the start-light gantry owns that lead-in). Fixes: a shared `isLiveOnTrack(telemetry)` helper (added to `@iracedeck/iracing-sdk` `telemetry-features.ts` next to `isPreGreen`; `IsOnTrack === true && IsReplayPlaying !== true`, the same signal the translator's `driver.firstOnTrack` gate uses) gates the race-formation flags + the whole start-light family on being live in the car (#829 later carved the countdown MARKS back out of this gate — the gantry lines keep it; see the #829 entry); and `one-lap-to-green` / `green-held` additionally gate on `!(getStandingStart() && isPreGreen(e.telemetry))` — a new `getStandingStart()` translator accessor (wraps `resolveStandingStart`, mirrors `getSessionType`/`getTrackDirection`) so the audio layer reads the standing flag without taking a direct `@iracedeck/iracing-sdk` session-info dependency. `isPreGreen` keeps the gate to the GRID (GetInCar/Warmup/ParadeLaps) so a rolling RESTART in a standing-start race (Racing state) still calls "one pace lap to go." The gates read `e.telemetry`, so the harness must drive on-track telemetry to test these (same as the #639 `hasPitLimiter`-gated callouts) — gating on the SCENARIO keeps the diff/event harness-firable. Reach for `isLiveOnTrack` whenever a callout should be silent while the user isn't driving (out of car / replay / session menu), and combine `getStandingStart()` + `isPreGreen` to distinguish a standing initial grid (no pace lap) from a rolling formation/restart.
- **Terse time-sensitive callout that must never defer or cut — pit-box count-in vs service readback** — issue #646. **SUPERSEDED by #758** (see the entry below), which reversed this pairing after field experience: the countdown is the time-critical callout on approach and now outranks + interrupts the readback. What survives from #646: `queueable: false` on the count-in (a stale number must drop, never defer/replay — that half of the design was right and is unchanged), and the general engine semantics it exercised (TRANSIENT-band fires drop when busy; a higher-weight `interrupt` fire cuts a running lower-weight one). Do NOT copy #646's readback-outranks-countdown weights — consult #758 for the current pairing.
- **Derive the real "one pace lap to go" moment from S/F crossings instead of trusting a mis-named sim flag bit, and gate progression callouts off post-race with an explicit state-set helper** — issue #657. Demonstrates **deriving a callout's trigger from a continuous measurement (lap-distance accrual across the start/finish line) when the sim flag bit that names the moment is asserted too early and too broadly to mean what it says**, plus **an explicit post-race state-set gate folded into the audio-scenarios predicate rather than trusting a flag's falling edge**. (1) **Root cause.** iRacing's `SessionFlags.OneLapToGreen` bit is really a "formation in progress" marker, not a literal "one lap to go": it's set for the *whole* parade (from grid release through every pace lap) and re-set again in cool-down, so the raw rising-edge trigger the old `one-lap-to-green` callout keyed on never coincided with the actual final-pace-lap moment — it fired at grid release (or at the qualifying-checkered grid formation, the leak #480 patched), and the bit's continued assertion / cool-down re-set made it mis-fire post-race. The bit's falling edge is equally untrustworthy. (2) **Replacement — distance-accrual S/F detection.** A new `diff/pace-laps.ts` tracks cumulative `LapDistPct` accrued since the car entered `ParadeLaps`, folding each tick's forward delta so the accrual survives the S/F wrap; it emits exactly when the start/finish crossing that **completes the first pace lap** occurs. A `>= 0.5` lap-accrued guard separates that completion crossing from the **grid-release crossing** (which sits at ~0 accrued — the car often starts a hair behind the line and crosses it immediately on release), so only the genuine end-of-first-pace-lap crossing fires. The diff is gated **rolling-only** (`resolveStandingStart` is false AND the car is in `ParadeLaps`) with a **green-not-held** guard, under the explicit **≤2-pace-lap assumption** — a one-pace-lap formation has no earlier lap to accrue against and stays silent (the green flies first), and the second-pace-lap case is the design target (fire as the final pace lap begins). (3) **Post-race gate — `isPostRace` helper.** A new `isPostRace(telemetry)` pure helper (in `@iracedeck/iracing-sdk` `telemetry-features.ts`, the mirror of `isPreGreen`) returns true for `SessionState ∈ {Checkered, CoolDown}` — and is folded into the audio-scenarios `liveRaceCar` predicate so the **entire race-progression flag family** (Crossed / One pace lap to go / Green held / Ten/Five to go) goes silent after the checkered, rather than relying on each individual bit's falling edge to stop talking. (4) **Rename + variant pool.** Every internal artifact named for the abandoned bit was renamed `one-lap-to-green` → `one-pace-lap-to-go` (event/scenario id, per-callout opt-in, clip pool, recorded clips) so the internal name no longer echoes the bit it no longer reads. The single terse line was then expanded into a **random no-repeat pool of five fuller variants** (and `green-held` likewise — five "green's coming" variants), regenerated via ElevenLabs. Note the pool picker is **uniform-random with a no-immediate-repeat guard** (the interpreter's `pickFromPool`), not a sequential rotation — the "rotate" wording in the pool docs (`pools.ts` / `flag-alerts.ts`) was corrected to "random" as part of this work. The callout still rides the `flag.*` event namespace and the shared `flag` family (preemption/weighting unchanged from #480/#652). Reach for this pattern when a sim flag bit is asserted too early or too broadly to mean what its name implies: derive the real moment from a continuous measurement (here, S/F crossings via cumulative `LapDistPct` accrual) with an episode-scoped guard (the `>= 0.5`-accrued separation of completion vs grid-release crossings), and gate a progression family off post-race with an explicit state-set helper (`isPostRace`, the `isPreGreen` mirror) folded into the shared live-gating predicate — never trust a "we're done" flag bit's falling edge.
- **Rolling-start field-rolling — fire a single-subject family off a `SessionState` ENTRY edge to fill the start-of-formation gap** — issue #660. Demonstrates **firing a callout off the ENTRY into a `SessionState` phase (`→ ParadeLaps`) rather than a flag bit or a continuous measure**, and **a temporally-distinct sibling on the same phase the existing family already touches**. The new `rolling-start` family (single subject `pace-car`) speaks once when the pace car starts rolling the field into the formation lap — filling the start-of-formation gap #480 left: a standing start gets the light-gantry sequence + numeric countdown, but a rolling start had nothing until "one pace lap to go" near the END of the formation lap (#657). A new `diff/rolling-start.ts` keeps the diff minimal — seed-on-first-tick + the `ParadeLaps` entry edge + a standing-start gate (`!resolveStandingStart`, so the gantry owns standing) — and emits `rollingStart.pace-car-moving.raised`. The single scenario carries `WEIGHT.SAFETY` and one opt-in (`calloutEnabledRollingStartPaceCar`, default ON) via `RollingStartCalloutId` / `ROLLING_START_CALLOUT_SETTING_KEYS`; `registerPitCrew` takes a `getRollingStartCalloutEnabled` resolver ahead of the start-light / master params. As with #480/#657 the race-session + in-car gating lives on the scenario `where:` (`liveRaceCar`), NOT in the diff, so the event stays harness-firable while only speaking when the user is live in a race. Reach for this pattern when the trigger is the ENTRY into a `SessionState` phase (not a flag bit or a continuous value) and the new callout is temporally distinct from an existing one already keyed on that phase — keep the diff to seed + entry edge + the start-type gate, and put the session/in-car gating on the scenario predicate.
- **Standing-start countdown: window widened, AI guard dropped, family trimmed** — issue #666 (refines #480). Demonstrates **removing an over-tight "trustworthiness proxy" gate once the underlying telemetry is confirmed reliable, and deleting a redundant belt-and-suspenders guard a more precise mechanism already covers**. #480 gated the numeric pre-start countdown on `SessionState===Warmup ∧ StartReady set` as a proxy for `SessionTimeRemain` being trustworthy (on the assumption the `GetInCar` value was a bogus fixed get-in buffer); in fact `SessionTimeRemain` is the real time-to-lights from `GetInCar` onward, so the window was widened to `standing ∧ SessionState ∈ {GetInCar, Warmup} ∧ ¬(StartSet∨StartGo) ∧ SessionTimeRemain>0` — opening at `GetInCar` (earlier marks no longer missed), dropping the `StartReady` requirement, and closing once the gantry `StartSet`/`StartGo` own the final moment. The separate `resolveIsAiRace` guard that suppressed all numbers in AI races was removed: the existing ceiling-seed (seed the highest reachable threshold from the first in-window `SessionTimeRemain`, fire only thresholds `≤ ceiling`) already prevents a compressed AI window from speaking a number it can't reach — validated by the standing-AI capture replay, which still emits no countdown (its window peaks at ~4.4 s, below the 10 s mark — the smallest surviving threshold) now via the ceiling-seed rather than the guard. The numeric countdown's weight dropped `WEIGHT.SAFETY`→`WEIGHT.NORMAL` (pre-race, low-stakes; the gantry `set`/`go` stay `CRITICAL` + `interrupt`). The scope then extended to **trim the family**: the `start-ready` gantry cue was removed end-to-end (the `startLight.start-ready.raised` bus event, its diff emission + `StartReady` dropped from the gantry mask, the scenario, pool, clip, and harness shortcut), and the `15` and `5` countdown marks were dropped — `StartCountdownSeconds` narrowed `60 | 30 | 15 | 10 | 5` → `60 | 30 | 10`, leaving gantry = `set` / `go` and marks = `60` / `30` / `10` (the kept clips re-recorded with fuller "race start" wording). The two opt-in keys (`calloutEnabledStartLights` / `calloutEnabledStartCountdown`) are unchanged. So beyond the gating/weight change it threads the bus catalog, translator diff, audio-scenarios (scenario / pool / `SCENARIO_ID_TO_START_LIGHT_ID`), audio-assets (config + clips + both manifests), the scenario harness, tests, and docs. Reach for this pattern when a gate was added as a conservative proxy for a data-quality worry that later proves false (widen to the real signal and delete the proxy), and when a second guard duplicates protection an existing mechanism already provides (delete it rather than keep both).
- **Pit window open/closed — a boolean-transition family with one opt-in over two directional scenarios, race-only + replay-only gated in the diff** — issue #655. Demonstrates **the value-change shape for a plain telemetry boolean**: a new `pitsOpen.changed { from, to }` bus event (the `radar.changed` / `pitService.statusChanged` shape) drives TWO scenarios that branch on `to` (`to === true` → opened, `to === false` → closed) rather than two separate events, both sharing `family: "pit-window"` so a caution's open→closed→open flurry preempts the stale in-flight family-mate cleanly. A minimal `diff/pits-open.ts` keeps the standard seed-then-transition shape (seed `lastPitsOpen` on the first tick, advance the baseline EVERY tick so a transition during a closed gate is absorbed not replayed, emit only on a real boolean change). **Gating is in the DIFF, not the scenario `where:`** (unlike the rolling-start / start-light families that gate on the scenario): the diff takes `isRaceSession` + `replayOnlySession` booleans (both already resolved once per tick in `handleTick` for the `session.changed` paths) and emits only in a live race — `PitsOpen` reads `false` in many non-race contexts, so the callout is race-only, and the **replay-only gate matters even though the diff runs after the main `IsReplayPlaying` guard** because a paused / frame-scrubbed replay can read `IsReplayPlaying === false` while `SimMode === "replay"` (#604's `isReplayOnlySession`), which would otherwise leak open/closed lines as the timeline jumps. The scenario `where:` therefore only discriminates direction (`e.data.to`), which keeps both scenarios firable from the scenario harness (it publishes the event directly with an explicit `to`). **One opt-in over both directions** (`PitWindowCalloutId = "pit-open-closed"` → `calloutEnabledPitOpenClosed`, the track-conditions "one opt-in / multiple scenarios" shape via `SCENARIO_ID_TO_PIT_WINDOW_ID`). **Scheduling: `weight: 65` + `interrupt: false` + `queueable: true`** — an explicit integer between `WEIGHT.NORMAL` (50) and `WEIGHT.SAFETY` (70) so flags (SAFETY) still win the bus, but the strategically-meaningful state never cuts an in-flight line and defers for replay rather than being dropped when a higher-weight fire is in flight. Reach for this pattern when the trigger is a raw telemetry boolean whose two edges are two callouts of one family: emit a single `{from,to}` value-change event, branch the scenarios on `to`, gate race-only + replay-only in the diff (passing the booleans `handleTick` already computes), and pick an explicit weight between the named bands when the callout slots *between* "ordinary" and "safety".
- **Start gantry heads-up moved to the Ready edge; countdown wording flattened + 90 s mark** — issue #673 (refines #666). Demonstrates **verifying which phase of a multi-bit sim light sequence a callout belongs on before trimming "redundant" bits**, and **keeping spoken countdown marks to a uniform terse template**. (1) **Root cause.** The standing-start procedure is Ready → Set → Go; #666 trimmed the `start-ready` cue end-to-end on the mistaken assumption Ready follows Set, leaving the heads-up line ("Lights. Get ready to go.") on the `StartSet` rising edge — but Set is the solid-red phase moments before Go, so the line played too late to act on. Fix: the line moved to the `StartReady` rising edge end-to-end (bus event, diff emission, scenario, pool, clip — the reverse of the #666 trim); **nothing is spoken at `StartSet`**. (2) **Standing-only gate on the Ready emission.** The rolling AI capture (2112) shows `StartReady` is held through Warmup→ParadeLaps in rolling starts too, where the rolling-start family (#660) owns the lead-in — so the diff gates the Ready edge on `resolveStandingStart` (exactly the gate the original #480 design had). `StartGo` stays ungated. (3) **Countdown window-close stays `¬(StartSet ∨ StartGo)`.** The standing capture (2056) shows `StartReady` is up while the numeric countdown runs — adding Ready to the close mask would suppress the Warmup-phase marks. This matters doubly because the family shares `family: "start-light"`: a countdown number firing after the Ready line would same-family-preempt it. With the close mask unchanged, a number can still fire after Ready in a long Warmup, which is acceptable ordering; the Ready line is `CRITICAL + interrupt` so it always wins the moment that matters. (4) **Terse uniform marks + a new threshold.** Every countdown mark was flattened to exactly "[N] seconds to race start." (trailing chatter like "Final checks now." dropped — reworded under the same `-01` entry names, so the generator's hash cache regenerates them in place), and a 90 s mark was added (`StartCountdownSeconds` widened to `90 | 60 | 30 | 10`, new pool/scenario/clip, harness `startCountdown(90)` button). Opt-in keys unchanged. Reach for this pattern when relocating a callout across a sim light sequence's phase bits: confirm the actual bit ORDER from a capture (not from the bit names), gate the new edge by start type if the bit is raised outside the procedure that owns it, and re-check every window/mask the old bit participated in rather than blindly swapping bit constants — one mask wants the new bit (edge detection), another must NOT get it (countdown window close).
- **Waving-yellow rework — treat a UI-mirroring flag bit's edges as "shown/no-longer-shown", validate the cleared edge with a translator hold, debounce re-raises with a scenario cooldown, and make the one-shot clear queueable** — issue #671 (refines #480). Demonstrates **handling iRacing flag bits that mirror what is SHOWN to the player rather than the world state**, and **the three-part CrewChief-derived recipe for announcing a "cleared"**. (1) **Root cause.** `YellowWaving` is player-relative and transient: it sets while the sim shows you the waved local yellow (approaching/in the affected zone) and drops when you pass out of it — even though the incident persists — then re-raises on the next approach (corroborated by CrewChief's mapper, SIMRacingApps, iFlag). So `flag.yellow.cleared` (fired the instant all yellow-ish bits drop) landed seconds after the waving raise, typically while the Voice bus was busy — and the non-queueable `YELLOW_CLEARED` scenario was silently dropped on any equal/higher-weight non-flag occupant (spotter calls are equal `WEIGHT.SAFETY`), which is why no "Yellow cleared." was ever heard; the per-zone re-raise also replayed the waving callout every pass. (2) **Translator hold (signal validation).** `diff/flags.ts` now emits `flag.yellow.cleared` only after the all-clear has been SUSTAINED for `YELLOW_CLEARED_HOLD_MS` (3 s — CrewChief's `timeBetweenYellowAndClearFlagMessages`), tracked via `state.yellowClearPendingSince` and cancelled if any yellow-ish bit re-raises inside the window; `diffFlags` gained a `now` param. This also stops the cleared from same-family-preempting the still-playing waving line. (3) **Scenario cooldown (announcement debounce).** `YELLOW_WAVING` / `CAUTION_WAVING` carry `cooldown: WAVING_FLAG_COOLDOWN_MS` (30 s — CrewChief's `timeBetweenYellowFlagMessages`), collapsing per-zone-pass re-raises to one callout per window; the event still fires every edge (harness unaffected). (4) **Queueable clear.** `YELLOW_CLEARED` is `queueable: true` (the FURLED precedent): the all-clear is a sustained state, so a fire deferred behind an equal-weight line replays at idle instead of being dropped. (5) **Edge-driven vs state-driven gating lesson.** CrewChief also gates its yellow talk on pit-lane/speed, but that is safe only because its logic is state-driven and re-evaluated every tick (a suppressed announcement self-heals); our scenarios are edge-driven, so a `where:`-suppressed one-shot event is lost forever — the pit-lane gate was deliberately NOT copied. Debounce belongs in the scenario `cooldown:` (announcement layer); validation belongs in the translator hold (signal layer). The two lines were also reworded (`yellow-waving-01` → "Yellow flag", `caution-waving-01` → "Caution is out. <break time="0.3s" /> Slow down and close up safely."). Reach for this recipe when a sim bit mirrors the player's flag display rather than the incident lifecycle: treat the falling edge as "no longer shown", hold-validate before announcing any "cleared", cooldown the raise scenario against re-display chatter, and mark one-shot state-confirmation callouts queueable.
- **Furled debounce + announced-only paired cleared** — issue #669. Demonstrates **debouncing a transient flag-bit RAISE in the diff and pairing it with a cleared callout gated on the raise having actually been announced**. Running briefly off track flashes iRacing's `Furled` bit for ~0.5 s, so the plain rising edge announced a furled black-flag warning that didn't exist. The rising edge now only ARMS a 1 s window (`FURLED_DEBOUNCE_MS`, exported from `diff/flags.ts`; `state.furledPendingAt` uses the `damagePendingAt` 0-sentinel shape) — `flag.furled.raised` fires once the bit has stayed set for the window (setting `state.furledAnnounced`), and the bit clearing meanwhile silently drops the pending announce. The new paired `flag.furled.cleared` fires on the falling edge ONLY when `furledAnnounced` was set, so a flicker fires *neither* raised nor cleared, and clear → re-raise announces again as a fresh episode. The debounce lives DIFF-SIDE, not as a scenario `triggerDelay` — a deferred scenario doesn't re-run its `where:`, so only the per-tick diff can re-check that the live bit is still set when the window elapses. Contrast with #671's yellow-cleared sustain hold, which validates the CLEARED edge (3 s all-clear hold); here the hold validates the RAISED edge, and the cleared edge needs no hold of its own because it's already gated on the announce. Connect-while-furled stays silent for the whole episode: the first-tick seed produces no rising edge (matching every other flag), so nothing arms and the later drop has no announce to pair with. The `FURLED_CLEARED` scenario mirrors `YELLOW_CLEARED` (`queueable: true` — a one-shot state confirmation replays at idle instead of being dropped) and slots into the existing flag family as a new `FlagCalloutId` member (`calloutEnabledFlagFurledCleared`) with no `registerPitCrew` or plugin change. Tests drive `diffFlags` directly with explicit `now` values across the window boundary (the `diff/pit-lane.test.ts` pattern — `translator.test.ts` reads the real `Date.now()` and can't simulate time). **Speak-time validity gate (manual-test follow-up):** the queueable FURLED fire can defer behind a longer call (incident points, readback) and the engine replays deferred fires WITHOUT re-running `where:` — so the diff-side debounce alone still let a stale raised line play after the warning was already withdrawn, followed by a stray "Black flag cleared.". The fix lives audio-side in `flag-alerts.ts`: the raised scenario wraps its ENTIRE sequence (radio frame included) in an `if:` step — `if:` predicates expand inside `executeFire`, i.e. at speak time including deferred replays, and an empty expansion plays nothing — which re-checks the LIVE `Furled` bit via `getLatestTelemetry()` (`null` → play: don't punish missing data, the #574 precedent, which also keeps the scenario harness firable without iRacing). The same predicate records `furledRaisedSpoken` (module state; `_setFurledRaisedSpoken` test seam); FURLED_CLEARED gates its `where:` on it (no fire without a spoken raise) and carries its own symmetric speak-time `if:` (CodeRabbit review catch) — a queued clear dies silently when the bit is BACK UP by the time the bus idles (the re-raise is debounced upstream, so no fresh raised fire has displaced the queued clear yet) — consuming the marker only when the clear actually plays, so the kept marker still pairs the eventual genuine drop. A fresh raised fire resets the marker so stale state can't leak across episodes (e.g. when a session change ate the falling edge). The missing-telemetry fallback INVERTS between the two gates (`furledBitUp(fallbackWhenUnknown)`): the raised gate treats unknown as still-up, the cleared gate as not-re-raised — each passes the value that keeps it from suppressing on missing data, which keeps both harness-firable without iRacing. Note the three gates sit at three layers for three reasons: the diff debounce validates the SIGNAL (bit flicker), the speak-time `if:` validates at PLAYBACK (queue latency), and the spoken marker pairs the two CALLOUTS (cleared follows audio, not events). Reach for this pattern when a flag bit can flash transiently without the condition it names actually holding: debounce the raise in the diff with a pending-timestamp + announced flag, and gate the paired cleared on the announced flag rather than on the bit's falling edge alone. And reach for the speak-time `if:`-wrap whenever a queueable callout's *validity* (not just freshness) can expire while it waits — `where:` runs at event arrival only; an `if:` around the whole sequence is the speak-time gate. **Escalation is not withdrawal (#846):** iRacing raises the actual black flag by CLEARING `Furled` and setting `Black` in one transition (captured: SessionFlags `0x100C0000` → `0x10050000`), so the furled falling edge alone cannot mean "warning withdrawn" — and worse, both callouts share `family: "flag"`, so the cleared fire emitted a moment after `flag.black.raised` same-family-preempted the black-flag line wholesale and the driver heard ONLY "Black flag cleared." right as the penalty landed. Fix at both layers: the diff consumes `furledAnnounced` silently (no cleared emit) when Black/Disqualify is up on the falling-edge tick — checked on the raw bits, since the `black` key is withheld from `current` when Disqualify is also set — and the FURLED_CLEARED speak-time `if:` additionally expands to silence (consuming the spoken marker; the episode is over for good) when a penalty bit is up, covering a legitimately-emitted clear that queued behind a longer line and was overtaken by the escalation before the bus idled. Both layers share one definition of "escalated" — `isPenaltyFlagActive` in `@iracedeck/iracing-sdk` `telemetry-features.ts` (the `isPreGreen`/`isPostRace` home) — so they can't silently diverge. When a sim raises a successor flag by clearing its precursor, a paired "cleared" must distinguish withdrawal from escalation at every layer that gates it.
- **Countdown outranks the readback + resume-from-interruption + inter-mark pending hold** — issue #758 (reverses #646). Demonstrates **flipping a scheduling pairing when field experience contradicts the original design**, and two new engine capabilities: **`resumable`** (an interrupt-cut queueable fire continues from the interrupted clip at idle-replay instead of re-firing from the top) and **`pendingHoldMs`** (a finishing fire holds the bus's pending drain so a train of related fires doesn't let the displaced line stutter back into its gaps). (1) **The reversal.** #646 made the pit-entry readback outrank and cut the count-in, with the count-in dropped whenever the bus was busy — so on short pit entries the readback occupied the whole approach and the driver parked with no countdown. #758 flips it: the count-in carries an explicit `PIT_BOX_COUNT_IN_WEIGHT = 30` (between `CHATTER` 10 and `NORMAL` 50 — the #655 between-bands precedent) + `interrupt: true`, so a mark cuts a playing readback immediately; the readback drops its `interrupt: true`. The count-in stays `queueable: false` (a stale number still must never replay late — the #646 half that survives) and still drops against `NORMAL`+ callouts (pit-status, flags, fuel-critical), so the reversal is scoped to the CHATTER band, not "countdown above everything". Priority is fixed — a configurable countdown-vs-details setting was considered and rejected as over-engineering. (2) **`resumable` (engine, opt-in per scenario).** When an `interrupt` cuts a queueable fire whose scenario declares `resumable: true`, `stashRunningIfQueueable` stashes the fire's FULL original expansion + the index of the op in flight (mapped through the `ActiveFire` source fields so a resumed fire cut AGAIN still stashes an absolute position). At idle-replay the engine RE-EXPANDS the sequence first: an unchanged expansion resumes at the interrupted clip — re-keying the walkie frame with the open tick when one was opened before the cut — while a changed expansion (the snapshot moved on while stashed) falls back to a full fresh replay, preserving the #481 freshness guarantee (the readback.test.ts interrupt-stash freshness test still passes untouched). The resume skips the cooldown re-check (a continuation, not a new fire). Opt-in matters: a blanket resume would break #669's furled speak-time `if:` validity gate, which relies on full re-expansion at replay — so `resumable` requires `queueable: true` (load-time validation) and is only for deterministic sequences with side-effect-free `if:` predicates; pool draws may differ between expansions and simply force the full-replay path. (3) **`pendingHoldMs` (engine, declared by the train).** The count-in marks are separate fires with ~1 s idle gaps; without a hold, the stashed readback would resume in the gap between "four" and "three" and be cut again — a stutter loop. Each mark declares `pendingHoldMs: PIT_BOX_PENDING_HOLD_MS` (2500 ms): `finishFire` arms a per-bus timer instead of draining immediately, a new fire taking the bus cancels it (re-arming at its own finish), and `stopAll` clears it — so the displaced readback plays once the mark train has been quiet for the window, landing while the car stops in the stall. The knob lives on the INTERRUPTING scenario (the train knows more of itself is coming), not on the displaced one — a one-shot interrupter (meatball) still lets the readback resume immediately at idle. Reach for this trio when a time-critical countdown must win a band-mate: explicit between-band weight + `interrupt` on the countdown, `resumable` on the long line it displaces, and `pendingHoldMs` on the train's members so the resume waits for the train to finish.
- **Checkered deferral — hold a whole-field flag bit until the player's own S/F crossing** — issue #771. The `Checkered` bit rises for the entire field the moment the session ends (quali clock expiry, race leader's finish) — most of a lap before the player takes the flag. The flag diff now tracks the player's scored crossings (`LapCompleted` increments) and — in qualifying and races — holds the checkered raise until the next one; practice/testing speaks immediately at the raise (the flag there just means the session is over, with no flag-taking lap — user-validated in #771 testing). Immediate-speak fallbacks for the deferring sessions: not in the car (`IsOnTrack !== true`), crossings untrackable (no `LapCompleted` — never hold a callout hostage to missing data), and parked in the pits in a NON-race session (`OnPitRoad`/`PlayerCarInPitStall` — the common end-of-quali case; no crossing is ever coming, while a race car in the pits still finishes by crossing the line and keeps deferring). Observed iRacing behavior shows the flag AHEAD of a car's crossing even for the winner, so the winner normally resolves through the deferral itself — right at the line; the winner grace (`FLAG_CROSS_GRACE_MS`) is a SAFETY NET for any timing where the bit instead lands at/after the leader's scored crossing, speaking immediately when the raise lands within 1 s of the player's own crossing while they LEAD A RACE — the leader flag is resolved by `handleTick` from the canonical live order (`calculateFrozenRacePositions`, per `.claude/rules/race-positions.md`; official `PlayerCarPosition` is the fallback only) and passed into the diff, and even a same-tick crossing counts only for the leader (a mid-pack car crossing on the raise tick is scored for one more lap; in qualifying the clock — not anyone's crossing — raises the flag, so the grace is race-gated). A pending fire resolves on the next crossing, on leaving the car, or (non-race) on driving into the pits; it dies silently when the bit drops, a session change wipes it with the rest of `TranslatorState`, and the translator's replay-tick wipe PRESERVES the pending cluster (`wipeStateForReplay`) — the flag seed deliberately leaves those fields alone — so a replay glance mid-deferral can't silently swallow the callout. `race.finished` (#569) is unaffected — it reads the raw bit, not the event. Reach for this pattern when a sim flag bit is raised for the whole field at once but the callout describes a per-driver moment: derive the player's own moment from a scored counter, enumerate the never-crosses fallbacks explicitly rather than holding forever, gate any "it was YOUR crossing" shortcut on the canonical leader in a race, and preserve pending deferral state across the replay wipe.
- **Two-stage white flag — split one flag callout into a raise-time heads-up and a crossing-time call, riding one opt-in** — issue #772 (builds on #771's crossing tracking). The White bit rises when the LEADER starts the final lap — well before the player reaches the line — so the single callout claimed "final lap" too early. In RACE sessions the raise now speaks a heads-up (reworded `flag-white-race` pool) and a new `flag.white-last-lap.raised` fires at the player's own scored crossing under the flag, latched once per white episode (`state.whiteLastLapFired`, re-armed when the bit drops). Four guards shape stage 2: it is RACE-ONLY in the diff AND the scenario (in practice/qualifying iRacing shows the white exactly when the PLAYER starts their own final lap — same tick as their crossing — so a session-agnostic split would swallow the only callout those sessions get); the checkered must not be up (the two bits can overlap for a tick on the finish crossing — the checkered call owns it); a `WHITE_LAST_LAP_MIN_GAP_MS` (6 s — hugging the ~4.5 s heads-up audio; iRacing shows the white while the leader is still ~5–10 s from the line, so even the leader's crossing usually lands past the guard) skips a crossing that lands mid-heads-up rather than same-family-preempting it mid-sentence; and should the bit ever land at/after the race leader's scored crossing, the leader skip (`leaderTookTheLine` — #771's race- and leader-gated grace) speaks only the last-lap line, a safety net mirroring the checkered grace (observed timing shows the raise LEADS the crossing). The scenario is `queueable: true` (the YELLOW_CLEARED precedent — the crossing is one-shot and the translator latch never re-fires, so a fire displaced by an equal-weight spotter call must replay at idle, not drop). It maps to the SAME `FlagCalloutId` (`"white"`) in `SCENARIO_ID_TO_FLAG_ID`, so both stages ride `calloutEnabledFlagWhite` with no schema/PI/plugin change (the #572 modifier precedent). Reach for this pattern when one flag's meaning splits into "shown" vs "applies to you now": keep both stages one subject (one opt-in), gate the split to the session type whose flag semantics actually separate the two moments, skip — don't delay — the second stage when the first is still playing, and let the flag bit's drop re-arm the crossing latch.
- **Pace-car-sourced crossing — track the actor that defines the moment, not the player** — issue #773 (refines #657). "One pace lap to go" fired at the PLAYER's S/F crossing, but the moment the field is committed to another pace lap is the PACE CAR's crossing — noticeably earlier from the mid/back of a long grid. The diff resolves the pace car once per formation attempt at the entry edge (`DriverInfo.PaceCarIdx`, falling back to a `CarIsPaceCar === 1` scan of `DriverInfo.Drivers`, stored as `paceLapPaceCarIdx`) and runs the same accrual/wrap heuristic on `CarIdxLapDistPct[paceCarIdx]`. The ACTIVE source (`paceLapSourceCarIdx`) is re-decided every tick: the pace car whenever it reports a valid distance AND is on the racing surface, the player otherwise — so a transient telemetry blip downgrades to the player and flips BACK when the data returns (a one-way downgrade would push the cue to the player's late crossing for the rest of the formation), and every flip RE-ANCHORS the baseline without folding a delta across the source jump (a naive switch reads as a phantom S/F wrap with enough accrued to false-fire). The pit gate reads `CarIdxTrackSurface` (`InPitStall`/`AproachingPits`) — NOT `CarIdxOnPitRoad`, which real telemetry shows unreliable (see `diff/race-finish.ts`) — because the pace car peels into pit lane at the end of the FINAL pace lap and pit lane passes the timing line at most tracks: tracking it there would false-fire "one pace lap to go" on a 1-lap formation moments before the green. Accrued distance carries across flips (both cars measure distance-since-formation-start; the 0.5-lap guard tolerates the offset). Reach for this pattern when a callout describes another car's action: source the continuous measurement from that car's per-car telemetry, keep the RESOLVED identity separate from the ACTIVE source so downgrades can recover, treat every source switch as a re-anchor (never a delta), and gate the foreign car's data on the reliable surface signal rather than the pit-road boolean.
- **Beyond-counted-laps suppression on the qualifying invalidation callout — don't let a clamped snapshot field collapse two distinct raw states** — issue #776 (extends #567). Demonstrates **preserving a raw-telemetry distinction that a snapshot normalization was erasing, and gating a callout on it with a fail-open check**. The #567 snapshot maps `lapsRemaining = max(0, SessionLapsRemainEx - 1)` ("attempts remaining AFTER the current invalidated lap") — but that clamp collapses raw `1` (ON the final counted lap: speak the out-of-laps tail) and raw `0` (BEYOND the counted laps: iRacing lets the driver keep circulating after a 2-lap qualifying's attempts are done, and those laps aren't timed attempts) into the same `0`, so every extra lap's first incident replayed "This lap will be invalidated. We're out of qualifying laps…" — announcing an invalidation of a lap that never counted, re-armed each lap by the per-lap latch. Fix: a new snapshot boolean `lapCounted` (computed translator-side as `!lapLimited || rawLapsRemaining === undefined || rawLapsRemaining > 0` — time-limited sessions and missing readings stay counted, the #574 don't-punish-missing-data precedent) and a suppression gate in `checkAndUpdateQualifyingLatch` beside the existing `lapStartedFromPits` one, checked as `=== false` so an untyped producer omitting the field fails OPEN to the callout. The suppression path does NOT touch the latch (the invariant every suppression path in that helper shares). The harness grew a ninth shortcut (`beyond-counted-laps-silent`) contrasting with the out-of-laps one — same `lapsRemaining: 0`, different `lapCounted`. Reach for this pattern when a snapshot builder normalizes/clamps a raw value for one consumer's convenience and a new consumer needs the pre-clamp distinction: keep the normalized field's contract stable and carry the lost distinction as an explicit boolean computed at the same source, gate with strict `=== false` when absence must not mean suppression, and keep suppression paths side-effect-free on the latch.
- **Out-of-car countdown — run a pre-start diff BEFORE the replay guard and split a family's gating by half** — issue #829 (refines #480/#666/#673). The standing-start numeric countdown ("Ninety/Sixty/Thirty/Ten seconds to race start") was silent whenever the driver wasn't in the car — but the countdown IS the "get in the car" reminder, so out-of-car (garage / session screen / in-session replay view, where iRacing reports `IsReplayPlaying: true`) is exactly where it matters. Two suppression layers, fixed separately. (1) **Translator:** the countdown was split out of `diffStartLights` into its own `diffStartCountdown` and moved BEFORE the replay guard (the #604 pre-guard pattern), gated on `!isReplayOnlySession` so a standalone saved replay stays silent; the gantry rising edges stay post-guard. Because the countdown now runs through replay-mode ticks, its state cluster (`startCountdownCeiling` / `startCountdownFired` / `startCountdownObserved`) joined the #771 checkered cluster in `wipeStateForReplay`'s preserved set — a re-seed at a garage↔car flip would lower the ceiling and silently drop a mark right at the boundary (`resetPerSessionState` still clears it at a real session change). (2) **Scenario:** the four countdown marks dropped `isLiveOnTrack` from their `where:` (keeping the race-session gate); the gantry lines keep the full `liveRaceCar` predicate — out of the car at lights-out means the start was missed, so "go, go, go" would be noise (a "we missed the start, we'll start from the pit lane" acknowledgment is a possible future callout). (3) **The split surfaced a hidden coupling:** the old combined diff's gantry first-tick seed ATE the tick whose `SessionTimeRemain` was a scheduled value the AI session collapsed right after (capture 2056: 262 s → 1.02 s) — that accident is what kept the validated capture silent; the standalone countdown reproduces it deliberately with its own first-tick silent observation (`startCountdownObserved`) so the ceiling anchors from the second reading on. Reach for this pattern when a callout must speak while the user isn't driving: split the diff at the guard boundary rather than weakening the guard, preserve the moved diff's state across the replay wipes, gate per-half at the scenario layer — and when splitting a combined diff, check what the shared first-tick seed was accidentally protecting (a validated capture replay is what catches it).
- **Laps-of-fuel-left estimation callouts — continuous-estimate crossings with margin, once-per-stint dedup, and refuel re-arm** — issue #838. Demonstrates **turning a continuously-recomputed estimate into discrete once-per-stint announcements**, and **injecting a numeric global setting into the translator as a live-read closure**. Replaced the never-registered legacy `fuel-warnings` family end-to-end: the `fuel.lapsRemaining.crossed` event, `diffFuel` (crude max-of-last-5 estimator, sticky thresholds, no refuel re-arm), and the unregistered scenario file are gone. The replacement event is `fuel.lapsLeft.crossed { count, lapsLeft }`, emitted by the new `diff/fuel-laps-left.ts`, which samples once per lap at the rising `LapDistPct` 0.5 crossing (race sessions, live in car), reads the #465 validated estimator via `getFuelStats()` over the Session Info default window so the spoken number tracks the Laps to Empty display, and computes `count = max(0, floor(raw − margin − remaining lap fraction))` — full laps completable AFTER the current one; `count === 0` is the dedicated "Box this lap for fuel." call. **Dedup is a single `lastAnnouncedCount` floor, not a per-threshold Set**: emit only when the new count is strictly below the floor — this gives descending-only announcements, no re-announce when fuel saving raises the estimate, and the no-stale-burst property (a multi-count drop speaks only the current count, the #480/#666 ceiling precedent) all from one field. **Refuel re-arm** clears the floor on a debounced per-tick `FuelLevel` increase (the #465 tracker’s 0.01 L epsilon); the floor and the previous-fuel-level tracker joined the `wipeStateForReplay` preserved cluster so a replay glance can’t re-announce and a garage refuel (which happens entirely inside replay-mode ticks) lands as one big positive delta on the first live tick back. **Margin plumbing:** the user margin (`fuelCalloutMarginLaps`, 0–3 laps, default 0.3) lives in deck-core but must be known inside the diff, so `initializeSimEventsIracing` gained an optional `options: SimEventsIracingOptions` param — plugins pass a live-read closure wrapped in `sanitizeFuelCalloutMarginLaps` (exported clamp/fallback) so a malformed persisted value can’t poison the estimate and sim-events-iracing stays deck-core-free; this is the first settings-backed knob injected into the translator. **Non-uniform opt-in defaults:** eleven per-count booleans (`calloutEnabledFuelLapsLeft10`…`1` + `…Box`) where 5/3/2/1/Box default true and the rest false — the PI checkbox builder carries a per-item `on` flag and OMITS the `default` attribute for off-items (the sdpi-checkbox truthy-attribute trap). Scenarios are `queueable: true` deliberately: the diff marks a count spoken when it EMITS, so a dropped fire would never re-announce — deferral is the only path that keeps the diff-side dedup and the audio in agreement. Reach for this pattern when a callout derives from a continuously-recomputed estimate rather than a sim state transition: sample at a fixed lap anchor, floor to a spoken integer, dedup on a monotone floor rather than a fired-set, and re-arm on the physical event (refuel) that starts a new episode.
- **PROXIMITY weight band — a proximity call must ALWAYS be heard, split urgency inside one imperative engine** — issue #867 (refines #651/#652). Demonstrates **adding a weight band above CRITICAL when a callout class must win the bus against everything**, and **splitting one var-driven engine's fires across two scenarios by urgency**. The spotter call sat at `WEIGHT.SAFETY` + `interrupt: true` + `queueable: false` — but an interrupt cut requires STRICTLY greater weight, so any in-flight equal-weight line (all flags, pit-approach/exit, rolling-start, stall-departure) or higher-weight line (meatball, fuel-critical, start gantry) silently dropped the "Car left"/"Car right" call, and `queueable: false` (correct — a stale proximity call must never replay late) meant no idle-replay either. A missed proximity call is the most dangerous silence the engineer can produce, so a new `WEIGHT.PROXIMITY = 120` band sits strictly above CRITICAL (AT CRITICAL would still drop against an in-flight CRITICAL line — equal weight never cuts) and the transition calls moved onto it. **Investigation lesson:** the live report blamed a lower-weight "fastest lap" line, but a reproduction test showed the engine cuts lower-weight lines correctly — the real blocker was necessarily an equal/higher-weight line; verify the scheduling matrix with a blocker-scenario test before assuming an engine bug. **The split:** not every spotter fire deserves the new band — at PROXIMITY the 1–10 s still-there reminder loop would repeatedly chop up CRITICAL lines (meatball is non-queueable and would be permanently lost; the start gantry would stutter). So the engine now fires TWO scenarios sharing `focusOwner: "spotter"` + the same `{ var: "spotterClip" }` shape: `pit-crew.spotter-call` (`WEIGHT.PROXIMITY`, `family: "spotter"`) for every car-presence transition (arrivals, escalations, swaps, the 2→1 de-escalation, three-wide) and `pit-crew.spotter-info` (`WEIGHT.SAFETY`, `family: "spotter-info"`) for "Clear." and the still-there loop. **The siblings deliberately do NOT share a family** (review catch): same-family preemption replaces the in-flight family-mate BEFORE any weight comparison, so a shared family would let a 1 s-cadence reminder tick wholesale-replace a still-playing PROXIMITY call — the exact inversion the band exists to prevent. With split families the call cuts a playing info line via weight + interrupt, an info fire arriving during a call clip drops harmlessly (the loop retries next tick), and each scenario still replaces its own in-flight fires. **The compensating queueable half** (review catch): raising a band above CRITICAL creates SYMMETRIC permanent-loss paths for non-queueable CRITICAL one-shots — the proximity call can now cut them mid-play (never stashed), and they can arrive during a spotter clip and be dropped (100 < 120, no defer) — so the meatball and the start-gantry ready/go lines became `queueable: true`, deferring/stashing for replay the moment the clip ends; their raises are one-shot edges that never re-fire, so replay-or-lose is the whole ballgame. The focus floor stays at `WEIGHT.SAFETY` — it governs what OTHERS may start during the episode (flags must still break through), a separate knob from the call's own weight. Reach for this pattern when a callout class must never be droppable (band strictly above everything it must beat, `interrupt: true`, `queueable: false`), and when one imperative engine's fires span two urgency classes: split them across sibling scenarios with SEPARATE families (shared family = weight-blind replacement) — and whenever a new band outranks an old top band, audit every non-queueable one-shot in the outranked bands for the new loss paths in BOTH directions.
- **Estimate-vs-race-length gating on the fuel callouts — don't suggest a pit stop the race can't use** — issue #866 (refines #838). Demonstrates **suppressing an estimate-derived callout when the estimate covers what's left of the race**, with the suppression layered by what's actually knowable per race format. The #838 diff computed `count` purely from fuel telemetry, so on the last lap of a race the tank already covered, `count === 0` fired the `WEIGHT.CRITICAL` + `interrupt` "Box this lap for fuel." — a pit suggestion that can't be acted on usefully, at the worst possible moment. Three suppression layers, all **diff-side** (this family's gating is diff-side by design; the scenarios keep filtering only on count): (1) **lap-limited coverage** — suppress when `count >= SessionLapsRemainEx − 1`; the two values have DIFFERENT shapes (the raw counter INCLUDES the current lap, #776, while `count` means full laps completable AFTER it), so the −1 bridges them, and the comparison uses the POST-margin count (consistent with what's spoken; the conservative margin makes suppression conservative too). The unlimited sentinel (`IRSDK_UNLIMITED_LAPS`, the shared iracing-native → iracing-sdk export — never a local 32767 copy), a missing field, or a negative reading is UNKNOWN — keep announcing, silence only on a positive "fuel covers the race" determination. (2) **The player's own final lap** — gate on the #772 white-flag crossing latch (`state.whiteLastLapFired`, set by `diffFlags`, which runs earlier in `handleTick` so it's same-tick fresh); this is the only reliable final-lap signal in a TIMED race, where the lap counter reads the sentinel. Full timed-race laps-remaining estimation (`SessionTimeRemain` / avg lap time) was deliberately deferred to #880, which shipped it (see its entry below) along with the dual-limit min, the sticky final-lap latch, and the enough-fuel reassurance. (3) **Post-race** — `isPostRace(telemetry)` (the #657 helper), because `SessionState` flips to Checkered at the LEADER's finish while the player still races to the line, and a cool-down "box this lap" is equally wrong. The final-lap and post-race gates (2)+(3) are deliberately **UNCONDITIONAL** — even an estimate saying the tank won't last to the line stays silent there, an accepted trade-off (#866 review): at the estimator's ±margin precision a final-lap shortage call is more often noise than a savable splash-and-dash, so only the earlier-lap comparison (1) demands a positive coverage determination. Critically, a suppressed emission does **NOT advance the dedup floor** (`fuelCalloutLastAnnouncedCount`): coverage is re-evaluated every lap, so a burn-rate spike that shrinks the estimate back below the race distance still announces; advancing the floor would silence that genuine emergency for the rest of the stint. Reach for this pattern when an estimate-driven callout recommends an action (pit, charge, repair) whose usefulness depends on how much race is left: compare the estimate against the remaining distance in the SAME units/shape as the spoken value, treat unknown race length as "keep announcing", layer a per-driver final-lap signal for formats where the distance isn't countable, and never let a suppression consume the dedup state that a later genuine emergency needs.
- **Corner-name callouts — open-vocabulary clip set from a bundled dataset, speed-scaled lead-point trigger** — issue #888. Establishes the **data-driven vocabulary** pattern: the spoken set comes from a committed dataset snapshot (`@iracedeck/track-data`, pruned from Lovely Sim Racing's lovely-track-data — CC BY-NC-SA 4.0 used under an explicit grant; **surfacing the LSR + Racing Circuits credit in the PI/docs is a grant condition, never remove it**), pre-generated as one clip per unique normalized name (`corner-names/<slug>-01`, ~439 clips; "T5"/"Turn 5" merge at snapshot-refresh time and the authoring script FAILS LOUDLY on digit names with no TTS rule). The `slug` rides in the event payload (`cornerName.approaching { name, slug }` — the catalog's first open-vocabulary payload) so audio never imports the dataset package, and a name whose clip doesn't exist simply aborts per #835 — dataset growth between releases degrades gracefully. Trigger is a **speed-scaled lead point** (`LapDistPct + clamp(Speed × leadSeconds / trackLengthMeters, 0, 0.2)`) swept tick-to-tick with interval-crossing semantics: silent first-tick seed (markers behind the lead point are simply never in a forward interval — no explicit seeding pass), once-per-marker-per-lap Set cleared when the lead point wraps S/F, signed-delta fold so reversing never fires, a 0.05 teleport re-anchor (reset-to-pits starts a fresh announced pass — correct for practice), multiple-markers-in-one-tick speaks only the one nearest the lead point (the no-stale-burst rule), and pit road **consumes crossings without announcing** (no chatter in the lane, no burst at pit exit). Practice/test-only gating lives in the DIFF (#655 shape — `classifySessionType === "practice"` covers Offline Testing) so the harness fires the event freely; the lead seconds are the second settings-backed knob injected into the translator (`SimEventsIracingOptions.getCornerCalloutLeadSeconds` + `sanitizeCornerCalloutLeadSeconds`, the #838 pattern). Scheduling: default weight, `interrupt: false`, `queueable: false` (a missed name must drop), `family: "corner-name"` (consecutive corners preempt — why the default lead is only 1 s), no radio frame (pit-box terse-delivery precedent). Reach for this pattern when a callout's vocabulary comes from DATA rather than an enum: normalize + slug at snapshot time, one clip per unique name, slug in the payload, #835 as the safety net.
- **Mid-session fresh-connect suppression on the intro briefs — gate the synthetic marker at the scenario, keep the synthesis intact** — issue #871 (refines #668/#568). The translator's fresh-connect synthesis (`session.changed { from: -1, to: N }`) deliberately fires during green for practice/qualifying (#668 — those sessions sit in `SessionState.Racing` their whole duration, and connecting in the garage mid-practice wants the brief), but nothing checked whether the driver was already driving — so a plugin restart (deck-host auto-update, crash recovery) mid-practice replayed the whole session brief to a driver already lapping. Fix at the SCENARIO layer, not the synthesis: the synthetic event is self-identifying (`from === -1`), so the `where:` predicates reject it — session-start when the envelope telemetry reads `IsOnTrack === true` (deliberately NOT `isLiveOnTrack`: its `IsReplayPlaying !== true` conjunct encodes "actively driving", but the question is "session already underway" — a connect tick with the in-session replay view open, or the #604 transient replay tick, would evade the gate; on-track state is the only meaningful "already underway" signal in practice/qualifying), race-start when `SessionState === Racing || isPostRace(e.telemetry)` (post-green; a pre-green grid restart still briefs — starting position + conditions are still actionable). Genuine transitions (`from >= 0`) are untouched, missing telemetry briefs (don't punish missing data), and the envelope telemetry is the synthesis-tick state — semantically "what was true at connect" (the #480 `e.telemetry` gate shape, which also keeps the events harness-firable: the harness attaches its live mock telemetry to every published event). The race-start gate is DEFENSE-IN-DEPTH — the translator already latches silently on a race + `Racing` connect (#568's "too late to brief") — but the scenario owning its own firing conditions covers harness-fired events and survives future synthesis changes. No new opt-in (#572 firing-condition-fix precedent). Reach for this pattern when a deliberately-synthesized event serves several consumers but one consumer's reaction is wrong in a sub-case the synthesis can't see: keep the synthesis intact, make the synthetic event self-identifying, and gate the one consumer's `where:` on the marker plus the disqualifying live state.
- **Timed-race coverage estimate + positive reassurance — close a suppression gap with a derived estimate, and speak the suppression instead of leaving silence** — issue #880 (completes #866's deferred work). Demonstrates **estimating a race's remaining laps when the sim doesn't provide them**, and **turning a silent suppression into a one-shot positive callout**. (1) **Reuse the tracker you already have.** The issue asked for "a lap-time tracker analogous to the #465 fuel tracker" — but the #465 tracker already records `lapTime` per lap under exactly the wanted validity gates (out/in/tow excluded, min-time floor), so `computeFuelStats` just grew `avgLapTime` over the same valid window. Check existing trackers' records before building a parallel one. (2) **Leader-aware timed estimate.** A timed race does NOT end at clock expiry — the leader takes the WHITE at their first crossing after expiry (up to one lap later) and the checkered one lap after that, so the leader's checkered upper bound is `SessionTimeRemain + 2 × leaderLap` (or ONE leader lap once the white is already up), and the player's remaining laps are `max(0, ceil((bound − remainingFraction × avgLap) / avgLap))` — the first implementation used `+ leaderLap` and the review caught the missing white lap — leader from the canonical live order (`race-positions.md`), pace from `CarIdxLastLapTime` (falls back to `CarIdxBestLapTime`, then the player's own average), injected into the diff as a translator closure (`resolveLeaderLapTimeS`). Overestimating remaining laps is the safe direction (warnings stay on), which also excuses green-pace averages under caution. Dual time+lap limits: covered = coveredByLaps OR coveredByTime — whichever limit ends the race sooner binds. (3) **`SessionLapsRemainEx` is LEADER-relative in races** — validated from six lapped-race captures in `local/` (`raw === SessionLapsTotal − leaderLapCompleted`; qualifying's player-relative reading, #776, does NOT carry over) — and that is the RIGHT quantity: a lapped car takes the checkered when the leader does, so under equal pace the leader's remaining laps ARE the player's remaining crossings, and multiclass overstates a slower player's remaining laps (conservative). Validate a counter's frame of reference from captures before "fixing" it. (4) **Sticky final-lap latch.** The #772 `whiteLastLapFired` re-arms when the White bit drops (correct for the flag family's episodes), so a caution replacing the white mid-final-lap re-opened the fuel suppression — a second, STICKY `playerFinalLapStarted` (set at the same sites, preserved across the replay wipe, cleared per-session AND by a GREEN rising edge — a green past the latched lap means overtime or a same-SessionNum admin restart, which the SessionNum-keyed reset never sees; the review caught the missing re-arm path) now carries the fuel gate. When two consumers need different lifetimes from one signal, give each its own latch instead of bending the shared one. (5) **Reassurance instead of silence.** `fuel.lapsLeft.raceCovered` fires at most once per stint, once the race is inside its last 10 laps (the BINDING known remaining distance ≤ the warning band ceiling — anchored on the RACE, not the tank: the original in-band trigger stayed silent through a surplus AI race and the first manual test revealed users expect the confirmation at 10-to-go regardless of surplus; the in-band case is a strict subset, so low-fuel behavior is unchanged); re-armed by refuels AND by any later real warning, so a burn-spike arc retracts correctly (warning → reassurance again). The dedup floor is still never advanced by suppression (#866 invariant). The truth guard doubles as hysteresis: the reassurance requires the UNCLAMPED count to cover the distance WITH A FULL LAP IN HAND (+1) — the count-0 clamp can hide a tank that won't finish even the current lap, "we have enough fuel" must never be false, and boundary noise must not alternate contradictory warn/reassure lines — and EMITTING it clears the warning dedup floor (the all-clear retracts the spoken warnings; a kept floor made the count-0 box call structurally unreachable after a box → reassure arc, a review catch). Rides the existing fuel-family opt-in closure as a new `FuelCalloutId` (`calloutEnabledFuelLapsLeftRaceCovered`, default ON) — no `registerPitCrew` signature change, no positional test churn. The pace averages exclude caution laps (`FuelLap.wasCaution` — full-course Caution/CautionWaving latched per segment invalidates the lap): pace-car laps deflate burn AND inflate lap time, compounding toward a false all-clear (review catch; also fixes Session Info's Laps to Empty under caution). And the leader-relative counter is off by one during the leader's white lap for a player who hasn't crossed yet — clamp lapsNeeded to ≥ 1 while the white flies pre-crossing. Reach for this pattern when a suppression's condition is user-meaningful: derive the missing distance estimate conservatively from what the sim does expose, spend one positive line closing the topic instead of leaving the driver to wonder why the warnings stopped — and when a positive callout asserts a prediction, demand a margin (a lap in hand) before speaking it, make speaking it retract the warnings it supersedes, and enumerate every path that can re-open the topic (refuel, later warning, race extension) as an explicit re-arm.

# Settings Window (#992)

The dedicated, full-size settings window that owns the plugin-global settings. It is **not a Property Inspector**: it is a page the plugin process serves over loopback and opens as a chromeless browser app window — but it reuses the PI framework verbatim (`sdpi-components.js`, `pi-components.js`, the `global-*.ejs` partials, every `ird-*` component), so a control that works in a PI works in the window unchanged. Website docs: `docs/getting-started/settings.md`.

Since #993 the server starts from the plugin's **store-ready startup block** (`ensureStarted()`, or the first `open()` if something gets there first) and runs for the process's lifetime, with its port + token — `_settingsChannel`, never persisted in the plugin's own store — mirrored to the deck host once per start (since phase 2) so Property Inspectors can bootstrap from it (the guarded full-cache mirror; see `global-settings.md`); on the fail-closed unreadable-file path that block never runs, so nothing is started and no channel is published. Every write the window makes lands in the plugin-owned settings file, now the single source of truth — and since phase 2 **every action PI talks to this same server** through its own bridge, so window ↔ PI ↔ PI edits all land in that one file and each surface sees the others' changes live (a PI whose bootstrap can't complete falls back to the deck host's copy — see `global-settings.md`).

## Architecture (where each piece lives)

| Piece          | Where                                                                                         | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| -------------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Request guard  | `deck-core/src/settings-window-guard.ts`                                                      | Pure. **Token first, then Origin, then cookie** (#993 phase 2): a valid launch token authorizes whatever the `Origin` — PIs are `file://` (Origin `null`) or host-served pages — while a token-less request must still match the loopback origin exactly (when it sends an `Origin` at all) before its `SameSite=Strict` cookie is even looked at, which is what keeps the DNS-rebinding mitigation on the cookie path. Never a CORS header.                                                                                                                                                                                                                                                                                                                                                                                         |
| Server         | `deck-core/src/settings-window-server.ts`                                                     | `node:http` + `ws`. Loopback only, **ephemeral port bound at plugin startup** (#993 — the lifetime changed, not the auth model), released on `close()`. Exposes `token` standalone for the `_settingsChannel` publisher. **Never throws out of a request or upgrade listener** — a malformed request target or percent-escape is a 400/404, the request handler is wrapped, and every accepted socket gets an `error` handler: an exception on any of those paths would end the plugin process. Serves the plugin's `ui/` as static assets (confined; traversal → 404), the WebSocket **fake host** at `/ws` (the window AND every PI), `GET /simhub/roles`. Never emits a CORS header. `onUpgradeDecision` reports every upgrade's verdict + `Origin` to the controller, which logs `Settings socket accepted/rejected …` at debug. |
| Session cookie | server                                                                                        | The URL token authenticates only the first navigation; the page's relative `<script src>` fetches and the WS upgrade carry no query string, so `/` sets `ird_sw_<port>=<token>; SameSite=Strict; HttpOnly` and later same-origin requests authenticate by cookie. The name is **port-scoped** because cookies are host-scoped and all three plugins' windows share one browser profile — a second plugin's server must not overwrite the first window's cookie.                                                                                                                                                                                                                                                                                                                                                                      |
| Fake host      | server (`attachFakeHost`)                                                                     | Speaks the Elgato PI protocol to sdpi: `getGlobalSettings` / `setGlobalSettings` / `didReceiveGlobalSettings`, `openUrl` (http(s) only), `logMessage`, `sendToPlugin`. Only the **global**-settings subset — every control on the page carries `global`. Like the real host it **never echoes a `setGlobalSettings` back to the socket that sent it** (the echo carries the PARSED cache, so a field the user just cleared would refill with its schema default under the cursor), and it skips a push whose payload equals the last one that socket received; other sockets and plugin-side writes are pushed as before, and an explicit `getGlobalSettings` is always answered.                                                                                                                                                    |
| Settings I/O   | injected `SettingsWindowHost`                                                                 | Bound in each `plugin.ts` to `getGlobalSettings` / `updateGlobalSettings` / `onGlobalSettingsChange`. The server never touches the singleton.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Commands       | `deck-core/src/settings-window-commands.ts`                                                   | `createSettingsWindowCommandHandler` validates + routes the page's `sendToPlugin` payloads: `windowBounds` (persist `_settingsWindowBounds`), `switchToProfile` (**explicit `deviceId` required** — the window has no implicit device), `audioPreview` (kind passed through as a string; the runner owns the allow-list), `openSettingsFolder` (#993 — takes no path; the handler passes its injected `storePath`), `enableFeature` (#1061 — the Getting Started page's one-press opt-ins; `enableFeatureWrites` derives the Race Engineer's gate AND startup-policy keys from `FEATURE_STARTUP_GATES` and writes them in ONE call, because writing the gate alone silently reverts on the next start for every pre-#1007 install), `voicePackRefresh` (#1034 — likewise takes nothing from the page; the plugin owns which directory is scanned).                                                                                                                                                                                                                                                                                                   |
| Launcher       | `deck-core/src/settings-window-launcher.ts` + `chromium-browser.ts`                           | Finds Edge/Chrome/Brave (App Paths registry, then well-known paths — memoized per process), spawns `--app=<url> --user-data-dir=%LOCALAPPDATA%\iRaceDeck\settings-window-profile --window-size/--window-position`, detached; falls back to the host's `openUrl`, including when the spawn fails **asynchronously** (`spawnApp` may return a promise, and the launcher awaits it). **`openUrl` resolving means "sent", not "opened"** — every adapter resolves on the write (Elgato) or is fire-and-forget (Mirabox/Ulanzi), and no host protocol reports whether a page appeared. So the launch result is a hand-off, the `settings-window-open` banner is a defensive net for an opener that outright rejects, and a machine with no usable browser reads as a success. Don't substitute a proxy signal such as "no Chromium found": the plain-tab fallback is a working, supported path. **The dedicated profile is load-bearing:** without it a running browser swallows the launch and ignores the size. One of the two `child_process` uses in the plugin (the other is Open folder below).                                                                                                                                                                                                                                              |
| Controller     | `deck-core/src/settings-window.ts`                                                            | One server per plugin, started by `ensureStarted()` at plugin startup or by the first `open()` — whichever comes first, idempotent and race-safe (concurrent first-callers share ONE in-flight start, and `close()` awaits a start already in flight so it can never orphan the server that start produces) — and **reused across opens** (a re-click never binds a second port). `ensureStarted()` resolves `{ port, token }`; the `onStarted` hook fires once per actual start with the same channel — the plugins bind it to `createSettingsChannelPublisher(...).publish` so a server that comes up late (after a failed startup bind) is still published and mirrored. Debug-logs the origin only, never the tokenised URL. Logs BOTH outcomes of both stages (#1005 — the plugins no longer each log the failures) and reports them through the `onStatus` hook, the only place that knows WHICH stage failed: `open()` starts the server first, so it rejects for a dead service and for an unopenable page alike, and a caller's `.catch` cannot tell them apart. A throwing hook is logged and swallowed.                                                                                                     |
| Settings store | `deck-core/src/settings-store.ts`                                                             | The plugin-owned settings file (#993) every window write ultimately lands in, via the injected host → `updateGlobalSettings`. The window never touches it directly; rules in `global-settings.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| Failure banner | `deck-core/src/settings-window-warning.ts` + `settings-window-warning-reporter.ts`          | The controller's `onStatus` turned into the `_warnings` PI banner (#1005): a pure evaluator plus a thin reporter the plugins wire as `onStatus`, mirroring the elevation-mismatch pair. Two ids, because **placement is keyed by id**: `settings-window-server` (`error`) is page-wide and stays in the PI's top strip, `settings-window-open` (`warning`) renders directly above the iRaceDeck Settings button (`open-settings.ejs`). They are NOT alternatives: a failed START raises BOTH — the error explains the page, and a third, short message marks the button unusable (it points at the error rather than repeating it, since both are on screen at once). The evaluator therefore returns a LIST, and the reporter reconciles it against `settingsWindowWarningScope`: a server report speaks for both records, an open report only for its own, so one press can never clear the page-wide error. **The button note cannot be raised by the press itself** — with no service there is no loopback channel, so the only thing that reaches a PI is the once-per-start host mirror (`publishUnavailable`), and anything raised after it has no route to the page. Hence the note ships with that mirror, before any press. The error text states the consequence users hit first: with no service a PI has no channel, falls back to the host copy, and every edit it makes is ignored by the plugin. Both records are RUN-SCOPED (`_warnings` is never persisted, #1014), so every start begins with a clean slate and the startup server report is what fills it — which is also why the window's own page withholds both ids: it is served BY that server, so neither can be true there. |
| Open folder    | `deck-core/src/open-folder.ts` + `ird-open-folder`                                            | `openFolderInExplorer(path)` spawns `explorer.exe /select,<path>` detached — the second `child_process` use beside the launcher, and deliberately not `openUrl` (http(s)-only everywhere). The path is always the plugin's own store path; nothing comes from the page.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Window bridge  | `pi-components/src/settings-window-bridge/` → `browser/settings-window-bridge.js`             | The window's own bridge, modelled on the Ulanzi one: monkeypatches `WebSocket` during sdpi's synchronous `connectElgatoStreamDeckSocket` so the socket opens `ws://<host>/ws?t=<token>`, drives connect with a synthetic identity, restores. **No frame translation** — the server speaks Elgato natively. Sets `window.__irdSettingsWindow = true`, watches bounds (polled — a window _move_ has no DOM event), and **closes the window when the socket dies** (the plugin/host went away).                                                                                                                                                                                                                                                                                                                                         |
| Page           | `iracing-actions/src/actions/settings-window/settings-window.ejs` → `ui/settings-window.html` | Compiled by the shared `piTemplatePlugin` like an action PI (it is not an action). Sidebar + panes; body grid rows are **named** because an `<ird-warnings>` is the first body child — the page places its own (marked `data-auto`, filtering out both settings-window ids, #1014), which is what stops `head-common` injecting an unfiltered one. The key-bindings table dedupes by setting key, so a binding `key-bindings.json` lists under two categories renders once.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| Getting Started | `pi-components/partials/settings-window-getting-started.ejs` + `iracing-actions/src/actions/data/getting-started.json` | The first-run page (#1061), same shape as What's New: authored once as the website's `docs/getting-started/first-steps.md`, parsed by `pnpm generate:getting-started-data` into a committed artifact all three plugins compile in, rendered offline with no fetch. The parser is a SIBLING of the changelog's, not a reuse — that one knows a release/category grammar this page does not have — and is strict for the same reason: an action id with no entry in the partial's map is a BUILD failure, since a missing control is invisible on both surfaces. The platform split runs between prose and controls, never between platforms: the Profiles prose renders everywhere and says "Stream Deck" in its own words, while the jump to that tab is gated, because the tab itself does not exist on Mirabox or Ulanzi. |
| What's New     | `pi-components/partials/settings-window-changelog.ejs` + `iracing-actions/src/actions/data/changelog.json` | The pane renders the notes THIS build ships, not the website (#1011). `pnpm generate:changelog-data` parses `packages/website/src/content/docs/changelog.mdx` — still the only place notes are authored — into the JSON, which the compile plugin picks up from `data/` like any other template data, so all three plugins get it with no rollup wiring. Bullet text is pre-escaped HTML, emitted raw. Offline, dark-themed, and the release matching the build's own `version` (pre-release suffix stripped) carries an INSTALLED badge; a build with no section says so. |
| Update check   | `deck-core/src/{changelog-html-sanitize,published-changelog,changelog-feed-client,update-check,update-check-service}.ts` + `pi-components/src/components/update-notice.ts` | The upstream half of the What's New pane (#1016). The plugin — never the page, which is cross-origin to the site with no CORS — fetches `https://iracedeck.com/changelog.json` (published by the website build from the SAME `scripts/lib/changelog-data.mjs` as the compiled-in artifact), sanitizes every bullet to `<strong>/<em>/<code>/<a href>`, keeps only DATED releases `semver.gt` the running version, caches 1 h (5 min after a failure, single-flight), and serves it at `GET /updates/status` — the `simHub` shape, gated on the `updateCheck` setting read live. `ird-update-notice` renders a banner + the newer releases into `#sw-changelog` and emits `ird-update-available`, which the page turns into the sidebar's UPDATE badge. It starts on DOMContentLoaded, not on upgrade: `pi-components.js` is a `<head>` script, so mid-parse neither the bridge's window flag nor the list it decorates exists yet. Every failure mode renders nothing. |
| PI bridge      | `pi-components/src/pi-settings-bridge/` → `browser/pi-settings-bridge.js`                     | The Elgato/Mirabox PI transport (#993 phase 2). Pre-defines `connectElgatoStreamDeckSocket` (sdpi calls the prior definition first, then opens its socket synchronously), so a one-shot `WebSocket` interceptor for both `ws://localhost:<port>` and `ws://127.0.0.1:<port>` hands sdpi a `PiSettingsBridgeSocket` — the real host socket plus the router — and restores the native constructor immediately. Injected into every action PI, never into `settings-window.html`.                                                                                                                                                                                                                                                                                                                                                       |
| Router         | `pi-components/src/settings-channel/router.ts` + `loopback.ts`                                | The pure state machine BOTH PI bridges run (`idle → bootstrapping → connecting → loopback`, `fallback`): one bootstrap `getGlobalSettings` on host open, `_settingsChannel` parsed out of the reply, then global-settings frames go to the plugin and its pushes to sdpi (host pushes dropped). `BOOTSTRAP_TIMEOUT_MS` (3 s) is armed on the bootstrap read and on every connect. `loopback.ts` is the one place that builds `ws://127.0.0.1:<port>/ws?t=<token>`. Rules: `global-settings.md`.                                                                                                                                                                                                                                                                                                                                      |
| Ulanzi bridge  | `pi-components/src/ulanzi-bridge/` → `browser/ulanzi-pi-bridge.js`                            | Translation (`translate.ts`) plus the same router inside `UlanziBridgeSocket` — its bootstrap read is the Ulanzi `getGlobalSettings`, the write's scope plus this PI's `actionid` (or `PI_READ_ACTIONID`) — same `PLUGIN_UUID`, same blank `key`, one field different: that host answers a read only when `actionid` is non-empty, and a blank one left every Ulanzi PI empty until #1039. Writes keep the blank plugin scope (#868) — the two directions do NOT share one scope. One bundle on Ulanzi: the PI bridge grew the behaviour rather than being stacked with a second script.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Injection      | `pi-components/build` `injectBridgeScriptPlugin` + `assertBridgeInjectionPlugin`              | Shared by all three plugins; the injector runs in `writeBundle`, the assertion in `closeBundle` and throws naming the offending file. **One bridge per page** — see the rule of that name below.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Open button    | `ird-open-settings` + `open-settings.ejs` (via `action-settings-footer.ejs` / `section-header.ejs`)                                | A centred block like `.ird-docs-link` (never an `sdpi-item` — that renders a label column). Labelled **iRaceDeck Settings** with a cog, in the factory's `compact` size (#1024). Routed per host by `onOpenSettingsRequest` on each **concrete** adapter (like `openUrl`, deliberately not on `IDeckPlatformAdapter`). Shares its DOM/CSS/click wiring with `ird-open-folder` through `defineSendToPluginButton` (`pi-components/src/components/send-to-plugin-button.ts`) — new one-shot `sendToPlugin` buttons go through that factory, and `icon`/`size` are how a consumer dresses one for its spot rather than growing a second button design. **Placement (#1024):** in an action PI the button closes the action's OWN settings, directly above `title-overrides` and OUTSIDE every surface-conditional wrapper — inside `#keypad-appearance` it would vanish on the dial surface; `action-settings-footer.ejs` owns that rule and `action-settings-footer-partial.test.ts` checks it across all 35 templates. `open-settings.ejs` is the button's single definition and carries the OPEN-failure banner directly above it (#1005) — that argument started as a distance one (the button then sat at the very bottom of a PI) and survives the move on its own terms: a warning about one control belongs beside that control. `head-common.ejs` gives the top strip the matching `except` so that banner renders exactly once per page; the server-failure banner is not filtered and stays in the strip.                                                                                                                                                                                                                                                                                                                                                                                 |

## Rules

1. **The window is the only home for plugin-global settings** (#1003). New global settings go in a `global-*.ejs` partial (or one of the split group partials below) and are included by `settings-window.ejs`. Never author a global control in an action Property Inspector: the one exception is `key-bindings-section.ejs`, which shows an action's OWN bindings because they are contextual to the action being configured — and even those also appear, in full, on the window's Key Bindings tab.
2. **`accordion.ejs` has two modes.** Pass `settingsWindow: true` at a top-level include and it renders a flat `.ird-sw-card` instead of `<details>`; EJS copies include data into nested includes, so the `global-*.ejs` partials forward it without changes. **Exception:** an `include()` written INSIDE the object literal of another include (`include('accordion', { content: include('global-common-diagnostics'), … })`) is evaluated in the page's own scope and gets nothing from the outer call — pass `{ settingsWindow: true }` to that nested include explicitly, or its `locals.settingsWindow` guards silently render nothing (the Diagnostics card's Open folder button, #993). `locals.foo` inside a partial reads include parameters (the compile plugin no longer shadows EJS's `locals`).
3. **The Common Settings groups are partials**: `global-common-{window-focus,mouse-pointer,simhub,dual-press,replay,chat,updates,diagnostics}.ejs` (items only), placed on tabs by the window. Same for the Race Engineer block: `race-engineer-settings.ejs`, `race-engineer-callouts.ejs` (owns its list computation), `setup-warning-patterns.ejs`, rendered as cards. Both sets had a second home in a PI until #1003 — the `global-common-settings.ejs` assembler (deleted) and `pit-crew.ejs`'s three accordions (removed). Tests render the REAL partials and pin every setting key.
   **Scripts follow the same split**: behaviour for these controls lives in `settings-window-scripts.ejs`, included by the window only. `head-common.ejs` keeps what BOTH surfaces need. Adding a global-control handler to `head-common.ejs` ships dead script to all 35 PIs — a test guards both directions.
4. **Write only what changed — an optimisation now, not a correctness guard.** sdpi saves its _whole_ global object on every change; the fake host diffs it against the current cache with deck-core's `sameValue` (string forms equal parsed values) and forwards only the differing keys, sparing a re-parse and a listener fan-out on every keystroke. Under #993's single writer a full-object write would also be correct — before it, this diff was load-bearing: a full write marked every key pending and let the #896 overlay misread a PI edit restoring a previous value as a stale echo (a window value rolling back a PI edit on hardware). Keep the diff; don't rely on it for correctness.
5. **Things a PI can't do the same way in the window** — each has a window-mode path keyed on `window.__irdSettingsWindow`:
   - SimHub reachability/roles: a direct fetch is cross-origin (no CORS) → `simhub-probe.ts` uses `GET /simhub/roles`, answered from the plugin's own SimHub service. `key-binding-input.ts` goes through the probe (a structural test forbids a private fetch).
   - Test buttons: no action context → `ird-audio-test` with a `preview="radar|voice|background"` attribute sends `sendToPlugin {event:"audioPreview", kind}`; the plugin runs `runAudioPreview` (`iracing-actions/src/audio/audio-previews.ts`). Since #1003 this is the ONLY preview path — the old route (a hidden `_test*` timestamp setting the Pit Crew action watched) went with the controls it served, and `ird-audio-test`'s `target` attribute was removed with it.
   - Profiles: no device → `ird-deck-device-select` (populated from the `_deckDevices` passthrough global the Elgato plugin publishes, like `_audioDeviceList`) + `ird-profile-switch device-from="<select id>"`; no choice → no send. The plugin routes it through `ElgatoPlatformAdapter.switchToBundledProfile` — the SAME dispatch the PI's accordion buttons use (suffix per #753, history per #762). Elgato build only (`profiles` flag), gated at compile time.
   - Bounds: the bridge reports a **settled** position (unchanged for one 1 s tick) plus a final report on `pagehide`, so a drag is one global-settings write, not one per second. A window that opens **fully off-screen** (bounds saved on a monitor since unplugged — Chromium replays `--window-position` unclamped) is pulled onto its nearest display with `moveTo` and a size-only report is persisted, so the next open gets default placement even if the move was refused.
   - Storage (#993): `global-common-diagnostics.ejs` shows the settings file's path on **both** surfaces (a disabled `sdpi-textfield` bound to `_settingsStorePath`); the **Open folder** button (`ird-open-folder` → `sendToPlugin {event:"openSettingsFolder"}`) renders only in the window (`locals.settingsWindow`), since that command is handled by `createSettingsWindowCommandHandler` — a PI's `sendToPlugin` goes to its action instead.
6. **Passthrough keys the window introduces**: `_settingsWindowBounds` (validated, sanity-bounded), `_deckDevices`, `_settingsStorePath` (#993), and `_voicePacks` (#1034 — the installed voice packs, **run-scoped** like `_warnings`: it describes what is on disk during this run, so persisting it would let a deleted pack reappear after a restart). All plugin-written; not user settings; they live in the plugin's store and reach the deck host's copy once per start through the guarded mirror write (`hostMirrorPayload`). `_settingsChannel` (`{ port, token }`) is the exception: per-process, so it is NOT persisted in the store (a stale copy from an older build is removed) and exists only in that host mirror — which is what lets a PI bootstrap the channel before it can talk to the plugin.
7. **Ulanzi normaliser trap:** `ulanzi-client.ts` `normalizeFrame` **drops** any `sendToPlugin` whose payload isn't a known marker. A new PI→plugin command on Ulanzi needs a marker added there (`openSettings` was), not just an adapter listener. It applies to commands sent from a **PI over the host socket** only: `openSettingsFolder` (and every other window command) arrives through the loopback fake host, which never passes through the Ulanzi normaliser, so it needs no marker. Global settings never needed one either — they are not `sendToPlugin` frames — and since phase 2 a bootstrapped PI sends them straight to the loopback fake host, so only global-settings frames on the bootstrap read and the fallback path still cross `translate.ts` at all (non-global frames always translate, bootstrapped or not).
8. **Shared browser helpers, never a private copy.** `window.__irdSettingsWindow` is read ONLY through `pi-components/src/components/settings-window-context.ts` (`SETTINGS_WINDOW_FLAG`, `inSettingsWindow()`); the bridge keeps its own copy of the name (separate bundle) and `build/settings-window-constants.test.ts` pins them equal. Components that send `sendToPlugin` frames go through `components/sdpi-client.ts` (`sendToPlugin`, `getStreamDeckClient`) — never a private client lookup.
9. Brand: accent `#ce2128`, the header shows `iracedeck-logo.png` (committed in `pi-components/browser/` beside the vendored sdpi file — that `.gitignore` is `browser/*` with negations, since git can't re-include a file under an ignored _directory_).
10. **The What's New pane has exactly one runtime** (#1011, #1016). Its own notes are compiled in and render newest-first with no script, no fetch and no scroll-into-view — that is what makes the pane work offline and never name a version the user does not have. The one exception is `ird-update-notice`, which asks the PLUGIN (never the website directly) whether something newer was published and prepends it. Anything it cannot answer renders nothing, so the compiled-in list never depends on it. Keep new behaviour out of this pane; if it needs the network, it goes through the plugin like this one does. Editing `changelog.mdx` without running `pnpm generate:changelog-data` leaves the pane stale, which the freshness test catches — see `@.claude/rules/changelog.md`.
11. **`open({ pane })` is a FRAGMENT, and the page's handler is a fail-soft** (#1061). The first-run open lands the window on a tab by appending `#<pane>`; a browser never sends a fragment to the server, so the token/`Origin`/cookie model is untouched by it, and the page reads it with `getElementById` (an id, not a selector), falling back to its default tab. Anything that is not a lowercase slug is dropped rather than interpolated — the value reaches a browser command line. Because that fallback is silent, renaming a pane would quietly land the first-run window on General: `GETTING_STARTED_PANE` (deck-core), `data-pane`/`id="pane-…"` (`settings-window.ejs`) and `SETTINGS_WINDOW_TABS` (`tabs.mjs`) must move together, and `tabs.test.mjs` pins both links.
12. **Never bind the same global setting twice on one page** (#1061). The fake host deliberately skips the echo to whichever socket wrote (see the *Fake host* row above), so a second control bound to that key is never told the value changed: it keeps displaying the old one and writes it straight back the moment it is touched, silently overwriting the choice just made. Both controls can be individually correct and individually tested — the defect exists only in their relationship, so a green suite says nothing about it. Send the reader to the tab that owns the setting instead (a `data-jump` button), which is what the Getting Started page does for `changelogNotification`.
13. **One bridge per page** — `pi-settings-bridge.js` on Elgato/Mirabox action PIs, `ulanzi-pi-bridge.js` on Ulanzi action PIs, `settings-window-bridge.js` on `settings-window.html` only. Two bridges on one page double-patch `WebSocket`. The build enforces it: `assertBridgeInjectionPlugin` runs in `closeBundle` on all three plugins and throws — naming the file — unless every generated page that loads sdpi carries its expected bridge exactly once, immediately before `<script src="sdpi-components.js">`, with no other known bridge present.

## Verifying a change

`pnpm build` then check the built `ui/` per plugin: the expected bridge script present exactly once and before `sdpi-components.js` in every PI page and in `settings-window.html`, the other bridges absent (the build asserts this, so a green build is the check), and the expected controls present (e.g. `grep -c '<ird-key-binding'` = 236 — one per unique setting; the two FFB force bindings that `key-bindings.json` lists under two categories are deduped). For a partial split, snapshot the compiled PI HTML before and prove it is byte-identical modulo whitespace after. Then the manual round-trip on hardware, with debug logging on: change a setting in the settings window → a PI showing that setting updates live, the log shows the update and `Settings saved: <path>`, and the settings file's **content** changes (not just its mtime — the load-time re-save refreshes mtime on every start); change a setting in a PI → the window updates live and the file changes again; a second PI open on the same setting follows both; restart the deck software → every value is still there, from the file. A PI that fell back to the host path logs `iRaceDeck: …` in its own DevTools console — that is the signal to check, not a silent wrong value.

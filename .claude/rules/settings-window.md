# Settings Window (#992)

The dedicated, full-size settings window that owns the plugin-global settings. It is **not a Property Inspector**: it is a page the plugin process serves over loopback and opens as a chromeless browser app window — but it reuses the PI framework verbatim (`sdpi-components.js`, `pi-components.js`, the `global-*.ejs` partials, every `ird-*` component), so a control that works in a PI works in the window unchanged. Website docs: `docs/features/settings-window.md`.

## Architecture (where each piece lives)

| Piece          | Where                                                                                         | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------- | --------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Request guard  | `deck-core/src/settings-window-guard.ts`                                                      | Pure. **Origin first, then token-or-cookie.** Origin is the DNS-rebinding mitigation; the token alone doesn't close that gap.                                                                                                                                                                                                                                                                                                                                                          |
| Server         | `deck-core/src/settings-window-server.ts`                                                     | `node:http` + `ws`. Loopback only, **ephemeral port bound lazily at the first window-open and kept for the plugin run** (reused by every later open — port and token — so a re-click never binds a second port; `close()` releases it but no plugin calls it today). Never throws out of a request/upgrade listener: a malformed request target or percent-escape is a 400/404, and every accepted socket has an `error` handler — an exception there would end the plugin process. Serves the plugin's `ui/` as static assets (confined; traversal → 404), the WebSocket **fake host** at `/ws`, `GET /simhub/roles`. Never emits a CORS header.                                                                                                                                                                                                                       |
| Session cookie | server                                                                                        | The URL token authenticates only the first navigation; the page's relative `<script src>` fetches and the WS upgrade carry no query string, so `/` sets `ird_sw_<port>=<token>; SameSite=Strict; HttpOnly` and later same-origin requests authenticate by cookie. The name is port-scoped because cookies are host-scoped and all three plugins' windows share one browser profile — a second plugin's server must not overwrite the first window's cookie.                                                                                                                                                                                                                             |
| Fake host      | server (`attachFakeHost`)                                                                     | Speaks the Elgato PI protocol to sdpi: `getGlobalSettings` / `setGlobalSettings` / `didReceiveGlobalSettings`, `openUrl` (http(s) only), `logMessage`, `sendToPlugin`. Only the **global**-settings subset — every control on the page carries `global`. Like the real host it never echoes a `setGlobalSettings` back to the socket that sent it (the echo carries the PARSED cache, so a field the user just cleared would refill with its schema default under the cursor), and it skips a push whose payload equals the last one that socket received; other sockets and plugin-side writes are pushed as before.                                                                                                                                                                                                                               |
| Settings I/O   | injected `SettingsWindowHost`                                                                 | Bound in each `plugin.ts` to `getGlobalSettings` / `updateGlobalSettings` / `onGlobalSettingsChange`. The server never touches the singleton.                                                                                                                                                                                                                                                                                                                                          |
| Commands       | `deck-core/src/settings-window-commands.ts`                                                   | `createSettingsWindowCommandHandler` validates + routes the page's `sendToPlugin` payloads: `windowBounds` (persist `_settingsWindowBounds`), `switchToProfile` (**explicit `deviceId` required** — the window has no implicit device), `audioPreview` (kind passed through as a string; the runner owns the allow-list).                                                                                                                                                              |
| Launcher       | `deck-core/src/settings-window-launcher.ts` + `chromium-browser.ts`                           | Finds Edge/Chrome/Brave (App Paths registry, then well-known paths), spawns `--app=<url> --user-data-dir=%LOCALAPPDATA%\iRaceDeck\settings-window-profile --window-size/--window-position`, detached; falls back to the host's `openUrl`. **The dedicated profile is load-bearing:** without it a running browser swallows the launch and ignores the size. The only `child_process` usage in the plugin.                                                                              |
| Controller     | `deck-core/src/settings-window.ts`                                                            | One server per plugin, started lazily, **reused across opens** (a re-click never binds a second port).                                                                                                                                                                                                                                                                                                                                                                                 |
| Bridge         | `pi-components/src/settings-window-bridge/` → `browser/settings-window-bridge.js`             | The fourth bridge, modelled on the Ulanzi one: monkeypatches `WebSocket` during sdpi's synchronous `connectElgatoStreamDeckSocket` so the socket opens `ws://<host>/ws?t=<token>`, drives connect with a synthetic identity, restores. **No frame translation** — the server speaks Elgato natively. Sets `window.__irdSettingsWindow = true`, watches bounds (polled — a window _move_ has no DOM event), and **closes the window when the socket dies** (the plugin/host went away). |
| Page           | `iracing-actions/src/actions/settings-window/settings-window.ejs` → `ui/settings-window.html` | Compiled by the shared `piTemplatePlugin` like an action PI (it is not an action). Sidebar + panes; body grid rows are **named** because `head-common` injects `<ird-warnings>` as the first body child.                                                                                                                                                                                                                                                                               |
| Injection      | `pi-components/build` `injectBridgeScriptPlugin`                                              | Shared by all three plugins. **Two bridges must never share a page**: on Ulanzi the PI bridge goes into every PI _except_ `settings-window.html`, which gets the settings-window bridge.                                                                                                                                                                                                                                                                                               |
| Open button    | `ird-open-settings` + `section-header.ejs` `openSettings` slot                                | A centred block like `.ird-docs-link` (never an `sdpi-item` — that renders a label column). Routed per host by `onOpenSettingsRequest` on each **concrete** adapter (like `openUrl`, deliberately not on `IDeckPlatformAdapter`).                                                                                                                                                                                                                                                      |

## Rules

1. **Reuse, don't fork.** New global settings go in a `global-*.ejs` partial (or one of the split group partials below) so both surfaces get them. Never author a global control only in the window or only in a PI.
2. **`accordion.ejs` has two modes.** Pass `settingsWindow: true` at a top-level include and it renders a flat `.ird-sw-card` instead of `<details>`; EJS copies include data into nested includes, so the `global-*.ejs` partials forward it without changes. `locals.foo` inside a partial reads include parameters (the compile plugin no longer shadows EJS's `locals`).
3. **The Common Settings groups are partials**: `global-common-{window-focus,simhub,dual-press,replay,chat,updates,diagnostics}.ejs` (items only). `global-common-settings.ejs` assembles them with headings into the PI's one accordion; the window places them on tabs. Same for the Race Engineer block: `race-engineer-settings.ejs`, `race-engineer-callouts.ejs` (owns its list computation), `setup-warning-patterns.ejs` — `pit-crew.ejs` wraps them in its three accordions (same `accordionId`s), the window in cards. Tests render the REAL partials and pin every setting key.
4. **Write only what changed.** sdpi saves its _whole_ global object on every change; the fake host diffs it against the current cache with deck-core's `sameValue` (string forms equal parsed values) and writes only the differing keys. A full-object write marks every key pending and lets the #896 overlay misread a later foreign write that restores a previous value as a stale echo — observed as a PI edit being rolled back to the window's value. Residual: the one key the user changed in the window can still hit this if flipped straight back in the PI before the host confirms; that is #896's design and the argument for #993.
5. **Things a PI can't do the same way in the window** — each has a window-mode path keyed on `window.__irdSettingsWindow`:
   - SimHub reachability/roles: a direct fetch is cross-origin (no CORS) → `simhub-probe.ts` uses `GET /simhub/roles`, answered from the plugin's own SimHub service. `key-binding-input.ts` goes through the probe (a structural test forbids a private fetch).
   - Test buttons: no action context → `ird-audio-test` with a `preview="radar|voice|background"` attribute sends `sendToPlugin {event:"audioPreview", kind}`; the plugin runs `runAudioPreview` (`iracing-actions/src/audio/audio-previews.ts`), the same runner the Pit Crew action calls.
   - Profiles: no device → `ird-deck-device-select` (populated from the `_deckDevices` passthrough global the Elgato plugin publishes, like `_audioDeviceList`) + `ird-profile-switch device-from="<select id>"`; no choice → no send. The plugin routes it through `ElgatoPlatformAdapter.switchToBundledProfile` — the SAME dispatch the PI's accordion buttons use (suffix per #753, history per #762). Elgato build only (`profiles` flag), gated at compile time.
   - Bounds: the bridge reports a **settled** position (unchanged for one 1 s tick) plus a final report on `pagehide`, so a drag is one global-settings write, not one per second. A window that opens **fully off-screen** (bounds saved on a monitor since unplugged — Chromium replays `--window-position` unclamped) is pulled onto its nearest display with `moveTo` and a size-only report is persisted, so the next open gets default placement even if the move was refused.
6. **Passthrough keys the window introduces**: `_settingsWindowBounds` (validated, sanity-bounded), `_deckDevices`. Both plugin-written; not user settings.
7. **Ulanzi normaliser trap:** `ulanzi-client.ts` `normalizeFrame` **drops** any `sendToPlugin` whose payload isn't a known marker. A new PI→plugin command on Ulanzi needs a marker added there (`openSettings` was), not just an adapter listener.
8. `window.__irdSettingsWindow` is read ONLY through `pi-components/src/components/settings-window-context.ts` (`SETTINGS_WINDOW_FLAG`, `inSettingsWindow()`); the bridge keeps its own copy of the name (separate bundle) and `build/settings-window-constants.test.ts` pins them equal. Components that send `sendToPlugin` frames use `components/sdpi-client.ts` (`sendToPlugin`, `getStreamDeckClient`) — never a private copy of the client lookup.
9. Brand: accent `#ce2128`, the header shows `iracedeck-logo.png` (committed in `pi-components/browser/` beside the vendored sdpi file — that `.gitignore` is `browser/*` with negations, since git can't re-include a file under an ignored _directory_).

## Verifying a change

`pnpm build` then check the built `ui/settings-window.html` per plugin: bridge script present exactly once and before `sdpi-components.js`; the _other_ bridge absent; the expected controls present (e.g. `grep -c '<ird-key-binding'` = 236 — one per unique setting; the two FFB force bindings that `key-bindings.json` lists under two categories are deduped). For a partial split, snapshot the compiled PI HTML before and prove it is byte-identical modulo whitespace after. Then the manual round-trip on hardware: change a setting in the window → visible in a PI, and back — and the plugin log (debug on) shows which write moved a key.

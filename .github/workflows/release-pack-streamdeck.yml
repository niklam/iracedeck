name: Release - Build Stream Deck Plugin

on:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: write

jobs:
  build-and-pack:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

      - name: Remove dev dependencies
        run: pnpm install --prod --no-frozen-lockfile

      - name: Pack Stream Deck plugin
        run: npx @elgato/cli@latest pack packages/iracing-plugin-stream-deck/com.iracedeck.sd.core.sdPlugin

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: streamdeck-plugin
          path: com.iracedeck.sd.core.streamDeckPlugin

      - name: Attach plugin to GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v3
        with:
          files: com.iracedeck.sd.core.streamDeckPlugin
